---
phase: quick-260616-m5n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/widgets/monthly_bar_chart.dart
  - lib/presentation/screens/history_screen.dart
autonomous: true
requirements:
  - CHART-07
must_haves:
  truths:
    - "Tapping a bar with data on MonthlyBarChart navigates to DayDetailScreen via push"
    - "Tapping a bar with zero data (transparent bar) does nothing — no navigation"
    - "MonthlyBarChart remains a pure presentation widget with no go_router import"
  artifacts:
    - path: "lib/presentation/widgets/monthly_bar_chart.dart"
      provides: "onBarTap callback parameter wired into BarTouchData touchCallback"
      contains: "final void Function(String dateKey)? onBarTap"
    - path: "lib/presentation/screens/history_screen.dart"
      provides: "onBarTap wired to context.push"
      contains: "context.push('/day/"
  key_links:
    - from: "lib/presentation/screens/history_screen.dart"
      to: "lib/presentation/widgets/monthly_bar_chart.dart"
      via: "onBarTap callback"
      pattern: "onBarTap:.*context\\.push"
    - from: "lib/presentation/widgets/monthly_bar_chart.dart"
      to: "onBarTap"
      via: "touchCallback in BarTouchData"
      pattern: "touchCallback"
---

<objective>
Wire monthly bar chart tap to DayDetailScreen navigation, closing the CHART-07 gap found in phase 18 verification.

Purpose: ROADMAP SC#1 and CHART-07 require that tapping a bar on the monthly chart (or a calendar day) navigates to DayDetailScreen. Only the calendar path was implemented. The bar chart's BarTouchData has no touchCallback and no navigation.

Output: MonthlyBarChart gains an optional `onBarTap` callback; HistoryScreen wires that callback to `context.push('/day/$dateKey')` with the same data-presence guard used for calendar day selection.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.planning/ROADMAP.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.planning/phases/18-day-detail-screen/18-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onBarTap callback to MonthlyBarChart and wire touchCallback</name>
  <files>lib/presentation/widgets/monthly_bar_chart.dart</files>
  <action>
Add an optional callback parameter to MonthlyBarChart:

  final void Function(String dateKey)? onBarTap;

Add it to the constructor's named parameters (after `targets`) and update the `const MonthlyBarChart({...})` initializer list.

In the `barTouchData` block (currently lines 178-204), add a `touchCallback` alongside the existing `handleBuiltInTouches: true` and `touchTooltipData`:

  touchCallback: onBarTap == null
      ? null
      : (FlTouchEvent event, BarTouchResponse? response) {
          if (event is! FlTapUpEvent) return;
          final spot = response?.spot;
          if (spot == null) return;
          final day = spot.touchedBarGroup.x;
          final dateKey = _toDateKey(DateTime(year, month, day));
          final total = monthTotals[dateKey] ?? 0;
          if (total > 0) {
            onBarTap!(dateKey);
          }
        },

The guard `total > 0` mirrors the onDaySelected guard in HistoryScreen (monthTotals check before push). `FlTapUpEvent` filters to completed taps only (not drag/hover). The `_toDateKey` helper is already defined at the top of the file. Do not import go_router — the widget stays a pure presentation widget.

No other changes to the widget's build method are needed.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter analyze lib/presentation/widgets/monthly_bar_chart.dart 2>&1 | grep -E "error|warning" | grep -v "^$" || echo "CLEAN"</automated>
  </verify>
  <done>MonthlyBarChart compiles cleanly; `onBarTap` parameter is optional (null-safe); touchCallback fires onBarTap only when tapped bar has total > 0; no go_router import in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire onBarTap in HistoryScreen to context.push</name>
  <files>lib/presentation/screens/history_screen.dart</files>
  <action>
In history_screen.dart, find the MonthlyBarChart widget call (currently lines 315-320):

  MonthlyBarChart(
    monthTotals: monthTotals,
    year: focused.year,
    month: focused.month,
    targets: targets,
  ),

Add the `onBarTap` parameter:

  MonthlyBarChart(
    monthTotals: monthTotals,
    year: focused.year,
    month: focused.month,
    targets: targets,
    onBarTap: (dateKey) => context.push('/day/$dateKey'),
  ),

No other changes needed. The data-presence guard is already enforced inside MonthlyBarChart's touchCallback (total > 0 check), so HistoryScreen does not need to re-check. The `context.push` call matches exactly the pattern used on line 234 in onDaySelected. go_router is already imported in history_screen.dart (line 3).
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter analyze lib/presentation/screens/history_screen.dart 2>&1 | grep -E "error|warning" | grep -v "^$" || echo "CLEAN"</automated>
  </verify>
  <done>HistoryScreen passes onBarTap to MonthlyBarChart; flutter analyze reports no errors on either file; `grep -c "onBarTap" lib/presentation/screens/history_screen.dart` returns at least 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bar tap -> navigation | User taps a chart bar; x-value (day int) is derived from fl_chart internal data, not from user text input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m5n-01 | Tampering | bar x-value -> dateKey construction | accept | x-value is an int from 1..31 produced by the widget's own barGroups loop; no external input crosses this boundary; DateTime(year, month, day) clamps invalid days naturally |
| T-m5n-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks:

1. `flutter analyze lib/presentation/widgets/monthly_bar_chart.dart lib/presentation/screens/history_screen.dart` — zero errors
2. `grep -c "onBarTap" lib/presentation/widgets/monthly_bar_chart.dart` — returns >= 2 (parameter declaration + touchCallback usage)
3. `grep -c "onBarTap" lib/presentation/screens/history_screen.dart` — returns >= 1
4. `grep "go_router" lib/presentation/widgets/monthly_bar_chart.dart` — returns nothing (widget stays pure)
5. `grep "touchCallback" lib/presentation/widgets/monthly_bar_chart.dart` — returns >= 1
</verification>

<success_criteria>
- MonthlyBarChart has `final void Function(String dateKey)? onBarTap` parameter
- BarTouchData in MonthlyBarChart has a touchCallback that calls onBarTap only when the tapped bar's total > 0
- HistoryScreen passes `onBarTap: (dateKey) => context.push('/day/$dateKey')` to MonthlyBarChart
- flutter analyze reports zero errors on both modified files
- MonthlyBarChart has no go_router import (pure presentation widget)
- CHART-07 gap from 18-VERIFICATION.md is closed
</success_criteria>

<output>
Create `.planning/quick/260616-m5n-wire-monthly-bar-chart-tap-to-daydetails/260616-m5n-SUMMARY.md` when done.
</output>
