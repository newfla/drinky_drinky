---
phase: quick
plan: 260616-e76
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/screens/home_screen.dart
autonomous: true
requirements:
  - HOME-01
must_haves:
  truths:
    - "On light theme, the circular progress ring background track is clearly visible at 0% progress"
    - "On dark theme, the ring background track appearance is unchanged"
    - "At any non-zero percentage the ring renders correctly in both themes"
  artifacts:
    - path: lib/presentation/screens/home_screen.dart
      provides: "CircularPercentIndicator with theme-aware background track color"
      contains: "brightness == Brightness.dark"
  key_links:
    - from: lib/presentation/screens/home_screen.dart
      to: percent_indicator CircularPercentIndicator
      via: backgroundColor parameter
      pattern: "backgroundColor:"
---

<objective>
Fix the `CircularPercentIndicator` background track (the empty portion of the ring) so it has
sufficient contrast on the light/white Material 3 theme.

Purpose: On light theme, `colorScheme.surfaceContainerHighest.withValues(alpha: 0.3)` blends into
the white background — the track is essentially invisible at 0% fill. The dark theme is fine.

Output: A brightness-aware `backgroundColor` expression that is visible in both light and dark
themes with no hardcoded colours.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.planning/STATE.md
@/Users/flavio.bizzarri/repo/drinky_drinky/lib/presentation/screens/home_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make progress ring background track theme-aware</name>
  <files>lib/presentation/screens/home_screen.dart</files>
  <action>
In the `_buildProgressSection` method (around line 138), replace the single `backgroundColor`
expression on `CircularPercentIndicator` with a brightness-aware value.

Current code (lines 138-139):
  backgroundColor:
      colorScheme.surfaceContainerHighest.withValues(alpha: 0.3),

Replace with a local variable computed just after the existing `goalMetColor` variable (line 121),
before the `return Column(...)`:

  final ringTrackColor = theme.brightness == Brightness.dark
      ? colorScheme.surfaceContainerHighest.withValues(alpha: 0.3)
      : colorScheme.surfaceContainerHighest;

Then update the `backgroundColor` parameter to use `ringTrackColor`:

  backgroundColor: ringTrackColor,

Rationale: On dark theme the faint 30%-opacity ghost track is subtle and appropriate. On light
theme the full `surfaceContainerHighest` tone (typically a medium-grey in Material 3 light
palettes) provides clear contrast against the white card/scaffold background — no hardcoded
colour needed, the token scales with any light colour scheme.

Do not change any other parameter of `CircularPercentIndicator`. Do not add imports.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter analyze lib/presentation/screens/home_screen.dart 2>&1 | grep -E "error|warning|hint" | grep -v "//"; echo "Exit: $?"</automated>
  </verify>
  <done>
`flutter analyze` reports no errors on home_screen.dart. The `backgroundColor` of
`CircularPercentIndicator` is `ringTrackColor`, which equals full-opacity
`surfaceContainerHighest` on light theme and 30%-opacity on dark theme.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI colour fix — no data crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e76-SC | Tampering | No new packages | accept | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
Run the app on a light-themed device/emulator, navigate to Home, observe the progress ring at
0% (no drinks logged today). The background track (empty arc) must be clearly visible as a grey
ring against the white background.

Switch to dark theme — confirm the ring still looks correct (faint ghost track).

`flutter analyze lib/presentation/screens/home_screen.dart` must produce no errors.
</verification>

<success_criteria>
- The progress ring track is visually distinct from the scaffold background on light theme
- Dark theme ring appearance is unchanged
- No new dependencies or hardcoded colour values introduced
- `flutter analyze` clean on the modified file
</success_criteria>

<output>
Create `.planning/quick/260616-e76-fix-empty-progress-ring-visibility-on-li/260616-e76-SUMMARY.md`
when done.
</output>
