---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.yaml
  - pubspec.yaml
  - ios/Podfile
  - android/app/build.gradle.kts
  - lib/data/database/tables/water_entries_table.dart
  - lib/data/database/tables/user_settings_table.dart
  - lib/data/database/tables/drink_presets_table.dart
  - lib/data/database/app_database.dart
  - lib/data/database/daos/water_entry_dao.dart
  - lib/data/database/daos/user_settings_dao.dart
  - lib/data/database/daos/drink_preset_dao.dart
  - lib/domain/entities/water_entry_entity.dart
  - lib/domain/entities/daily_progress.dart
  - lib/domain/entities/user_settings_entity.dart
  - lib/domain/entities/drink_preset_entity.dart
  - lib/data/repositories/water_repository.dart
  - lib/data/repositories/settings_repository.dart
  - lib/core/providers/database_provider.dart
  - lib/core/providers/repository_providers.dart
  - lib/core/providers/stream_providers.dart
  - lib/core/router/app_router.dart
  - lib/presentation/screens/home_screen.dart
  - lib/presentation/screens/history_screen.dart
  - lib/presentation/screens/settings_screen.dart
  - lib/main.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Drift database initializes with correct DateTime-as-text storage mode"
    - "DAOs can create, read, and query water entries, user settings, and drink presets"
    - "Repositories expose reactive streams that emit updates when underlying data changes"
    - "Riverpod providers are wired to repositories so widgets can watch data without direct DB access"
    - "App launches with GoRouter serving three routes and data flowing from DB through providers to screens"
  artifacts:
    - path: "build.yaml"
      provides: "Drift code generation config with store_date_time_values_as_text: true"
      contains: "store_date_time_values_as_text: true"
    - path: "lib/data/database/app_database.dart"
      provides: "AppDatabase with migration seeding"
      contains: "schemaVersion => 1"
    - path: "lib/data/database/daos/water_entry_dao.dart"
      provides: "Water entry CRUD and reactive queries"
      contains: "watchEntriesForDate"
    - path: "lib/data/repositories/water_repository.dart"
      provides: "Stream gateway transforming Drift rows to Freezed entities"
      contains: "WaterEntryEntity"
    - path: "lib/core/providers/database_provider.dart"
      provides: "Singleton DB provider with keepAlive"
      contains: "Riverpod(keepAlive: true)"
    - path: "lib/core/providers/stream_providers.dart"
      provides: "StreamProviders wrapping repository streams"
      contains: "todayWaterEntries"
    - path: "lib/main.dart"
      provides: "App entry point with ProviderScope and GoRouter"
      contains: "ProviderScope"
  key_links:
    - from: "lib/core/providers/stream_providers.dart"
      to: "lib/data/repositories/water_repository.dart"
      via: "ref.watch(waterRepositoryProvider)"
      pattern: "ref\\.watch\\(waterRepositoryProvider\\)"
    - from: "lib/data/repositories/water_repository.dart"
      to: "lib/data/database/daos/water_entry_dao.dart"
      via: "db.waterEntryDao method calls"
      pattern: "_db\\.waterEntryDao"
    - from: "lib/core/providers/database_provider.dart"
      to: "lib/data/database/app_database.dart"
      via: "AppDatabase constructor"
      pattern: "AppDatabase\\(\\)"
---

## Phase Goal

**As a** hydration-conscious user, **I want to** have a fully wired persistence layer with reactive data streams, **so that** all subsequent features can read and write water intake data without touching SQLite directly.

<objective>
Create the complete Drinky Drinky Flutter project from scratch: scaffold, Drift database with three tables, DAOs, Freezed domain entities, repositories with reactive streams, Riverpod providers, GoRouter navigation skeleton, and a working main.dart that launches the app. This plan implements all 16 locked decisions (D-01 through D-16 minus D-16 which is tests in Plan 02).

Purpose: Establish the walking skeleton -- the thinnest possible end-to-end stack that proves DB writes flow through repositories and providers to UI screens. Every subsequent phase builds on this foundation without renegotiating architectural decisions.

Output: A Flutter project that compiles, runs build_runner successfully, launches on a device/simulator, and displays placeholder screens wired to live Drift data via Riverpod.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Flutter project with critical Drift config and all dependencies</name>
  <files>
    build.yaml,
    pubspec.yaml,
    ios/Podfile,
    android/app/build.gradle.kts
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1: build.yaml, Installation section for pubspec.yaml, Pitfall 1: build.yaml before code generation, Pitfall 6: iOS Podfile)
    .planning/phases/01-data-foundation/01-CONTEXT.md (D-01 through D-04, D-13)
  </read_first>
  <action>
    Run flutter create --org com.bizzarri --project-name drinky_drinky --platforms ios,android per D-04 in the repo root directory. If the command fails because Flutter is not in PATH, halt and report.

    IMMEDIATELY after flutter create, before ANY other file changes, create build.yaml in the project root per D-13 with targets.$default.builders.drift_dev.options.store_date_time_values_as_text set to true. This is the IRREVERSIBLE decision -- if code generation runs without this file, the storage mode is locked to integer timestamps.

    Replace the generated pubspec.yaml dependencies and dev_dependencies sections with the exact versions from 01-RESEARCH.md Installation section. Keep the flutter sdk dependency. Set the Dart SDK constraint to sdk: >=3.10.0 <4.0.0. Set the app name to drinky_drinky and description to "A hydration tracking app that helps you meet your daily water goal."

    Update iOS minimum deployment target per D-02: edit ios/Podfile to set platform :ios, '16.0'. Also search ios/Runner.xcodeproj/project.pbxproj for IPHONEOS_DEPLOYMENT_TARGET and update all occurrences to 16.0.

    Update Android configuration per D-03: in android/app/build.gradle.kts (or build.gradle if .kts does not exist), set minSdk to 26 and compileSdk to 36.

    Run flutter pub get to verify all dependencies resolve. If pub get fails, diagnose and fix version conflicts before proceeding.

    Create the layer-first folder structure per D-12 by creating empty .gitkeep files in each directory:
    - lib/data/database/tables/
    - lib/data/database/daos/
    - lib/data/repositories/
    - lib/domain/entities/
    - lib/presentation/screens/
    - lib/core/providers/
    - lib/core/router/
  </action>
  <verify>
    <automated>cd drinky_drinky 2>/dev/null || true; grep -c "store_date_time_values_as_text: true" build.yaml && flutter pub get --dry-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - build.yaml exists at project root and contains exactly the line "store_date_time_values_as_text: true" under targets.$default.builders.drift_dev.options
    - pubspec.yaml lists drift: ^2.33.0, drift_flutter: ^0.3.0, flutter_riverpod: ^3.3.1, riverpod_annotation: ^4.0.2, go_router: ^17.3.0, freezed_annotation: ^3.1.0, json_annotation: ^4.12.0, path_provider: ^2.1.5 in dependencies
    - pubspec.yaml lists drift_dev: ^2.33.0, build_runner: ^2.15.0, riverpod_generator: ^4.0.3, freezed: ^3.2.5, riverpod_lint: ^3.1.3, custom_lint: ^0.8.1 in dev_dependencies
    - ios/Podfile contains "platform :ios, '16.0'"
    - android/app/build.gradle.kts (or .gradle) contains minSdk = 26 (or minSdkVersion 26) and compileSdk = 36 (or compileSdkVersion 36)
    - flutter pub get completes with exit code 0
    - Directory structure exists: lib/data/database/tables/, lib/data/database/daos/, lib/data/repositories/, lib/domain/entities/, lib/presentation/screens/, lib/core/providers/, lib/core/router/
  </acceptance_criteria>
  <done>Flutter project exists at repo root with com.bizzarri.drinkydrinky bundle ID, build.yaml locks DateTime-as-text storage, all Phase 1 dependencies resolve, iOS targets 16.0, Android targets API 26, and the layer-first folder structure is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Drift schema, DAOs, Freezed entities, and run code generation</name>
  <files>
    lib/data/database/tables/water_entries_table.dart,
    lib/data/database/tables/user_settings_table.dart,
    lib/data/database/tables/drink_presets_table.dart,
    lib/data/database/app_database.dart,
    lib/data/database/daos/water_entry_dao.dart,
    lib/data/database/daos/user_settings_dao.dart,
    lib/data/database/daos/drink_preset_dao.dart,
    lib/domain/entities/water_entry_entity.dart,
    lib/domain/entities/daily_progress.dart,
    lib/domain/entities/user_settings_entity.dart,
    lib/domain/entities/drink_preset_entity.dart
  </files>
  <read_first>
    build.yaml (confirm store_date_time_values_as_text: true is present -- do NOT proceed if missing)
    .planning/phases/01-data-foundation/01-RESEARCH.md (Patterns 2-5 for exact table, DAO, AppDatabase, and Freezed syntax)
    .planning/phases/01-data-foundation/01-CONTEXT.md (D-05 through D-08 for default seed values, D-10 for Freezed, D-14 for date_key, D-15 for table schemas)
  </read_first>
  <action>
    PREREQUISITE CHECK: Read build.yaml and confirm store_date_time_values_as_text: true exists. If it does not, STOP and fix build.yaml first. This is the irreversible constraint.

    Create three Drift table definition files per D-15:

    water_entries_table.dart: Define WaterEntries extending Table with columns id (integer().autoIncrement()), amountMl (integer()), loggedAt (dateTime()), dateKey (text()). Add a @TableIndex annotation named idx_water_entries_date_key on the dateKey column per D-14. Import package:drift/drift.dart.

    user_settings_table.dart: Define UserSettings extending Table with columns id (integer().autoIncrement()), dailyTargetMl (integer().withDefault(const Constant(2000))), notificationIntervalMinutes (integer().withDefault(const Constant(60))), dndStartHour (integer().withDefault(const Constant(23))), dndStartMinute (integer().withDefault(const Constant(0))), dndEndHour (integer().withDefault(const Constant(7))), dndEndMinute (integer().withDefault(const Constant(0))), dndEnabled (boolean().withDefault(const Constant(true))). All defaults per D-05, D-07, D-08.

    drink_presets_table.dart: Define DrinkPresets extending Table with columns id (integer().autoIncrement()), amountMl (integer()), sortOrder (integer()).

    Create three DAO files following Pattern 4 from RESEARCH.md:

    water_entry_dao.dart: @DriftAccessor(tables: [WaterEntries]) class WaterEntryDao with methods insertEntry (accepts WaterEntriesCompanion), watchEntriesForDate (accepts String dateKey, returns Stream of List, ordered by loggedAt ascending), watchTotalForDate (accepts String dateKey, returns Stream of int using amountMl.sum()), deleteLastEntry (finds most recent by loggedAt descending, deletes it, returns count), watchEntriesInRange (accepts startDateKey and endDateKey strings, returns Stream of List). Include part directive for water_entry_dao.g.dart.

    user_settings_dao.dart: @DriftAccessor(tables: [UserSettings]) class UserSettingsDao with methods watchSettings (returns Stream of single row where id=1 via watchSingle), getSettings (returns Future of single row where id=1), updateSettings (accepts UserSettingsCompanion, updates where id=1). Include part directive.

    drink_preset_dao.dart: @DriftAccessor(tables: [DrinkPresets]) class DrinkPresetDao with methods watchAllPresets (returns Stream of List ordered by sortOrder ascending), updatePreset (accepts id and amountMl, updates matching row). Include part directive.

    Create app_database.dart following Pattern 3 from RESEARCH.md: @DriftDatabase annotated with tables [WaterEntries, UserSettings, DrinkPresets] and daos [WaterEntryDao, UserSettingsDao, DrinkPresetDao]. Constructor accepts optional QueryExecutor for testing. Static _openConnection uses driftDatabase with name 'drinky_drinky' and DriftNativeOptions with getApplicationSupportDirectory. schemaVersion returns 1. MigrationStrategy.onCreate calls m.createAll(), then inserts default settings row (UserSettingsCompanion.insert() with no args since all columns have defaults), then batch-inserts four drink presets with amounts 200, 300, 400, 500 and sortOrder 0, 1, 2, 3 per D-06. beforeOpen enables foreign keys via PRAGMA. Include part directive for app_database.g.dart.

    Create four Freezed domain entity files following Pattern 5 and Freezed 3.x abstract class syntax per D-10:

    water_entry_entity.dart: @freezed abstract class WaterEntryEntity with _$WaterEntryEntity, factory constructor with required int id, required int amountMl, required DateTime loggedAt, required String dateKey. Include part directive for water_entry_entity.freezed.dart.

    daily_progress.dart: @freezed abstract class DailyProgress with _$DailyProgress, factory constructor with required int totalMl, required int targetMl, required List of WaterEntryEntity entries, required String dateKey. Include part directive.

    user_settings_entity.dart: @freezed abstract class UserSettingsEntity with _$UserSettingsEntity, factory constructor with required int dailyTargetMl, required int notificationIntervalMinutes, required int dndStartHour, required int dndStartMinute, required int dndEndHour, required int dndEndMinute, required bool dndEnabled. Include part directive.

    drink_preset_entity.dart: @freezed abstract class DrinkPresetEntity with _$DrinkPresetEntity, factory constructor with required int id, required int amountMl, required int sortOrder. Include part directive.

    Run dart run build_runner build --delete-conflicting-outputs. This generates all .g.dart and .freezed.dart files. Verify build_runner completes with 0 errors. If errors occur, fix the source files and re-run.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -10 && test -f lib/data/database/app_database.g.dart && test -f lib/domain/entities/water_entry_entity.freezed.dart && echo "CODEGEN_OK"</automated>
  </verify>
  <acceptance_criteria>
    - build_runner completes with 0 errors and generates .g.dart files for app_database, water_entry_dao, user_settings_dao, drink_preset_dao
    - build_runner generates .freezed.dart files for water_entry_entity, daily_progress, user_settings_entity, drink_preset_entity
    - water_entries_table.dart defines WaterEntries table with id, amountMl, loggedAt, dateKey columns and a @TableIndex on dateKey
    - user_settings_table.dart defines UserSettings table with all 8 columns, each with correct defaults matching D-05/D-07/D-08
    - drink_presets_table.dart defines DrinkPresets table with id, amountMl, sortOrder columns
    - app_database.dart MigrationStrategy.onCreate seeds default settings row and four preset rows (200/300/400/500ml)
    - All Freezed entities use abstract class syntax (not non-abstract class)
    - Domain entities use Entity suffix (WaterEntryEntity, UserSettingsEntity, DrinkPresetEntity) to avoid Drift row class name collision
  </acceptance_criteria>
  <done>All Drift tables, DAOs, AppDatabase with migration seeding, and Freezed domain entities compile and generate code successfully. The database schema matches D-15, defaults match D-05/D-06/D-07/D-08, dateKey column exists per D-14, and DateTime storage uses text mode per D-13.</done>
</task>

<task type="auto">
  <name>Task 3: Wire repositories, Riverpod providers, GoRouter, and main.dart</name>
  <files>
    lib/data/repositories/water_repository.dart,
    lib/data/repositories/settings_repository.dart,
    lib/core/providers/database_provider.dart,
    lib/core/providers/repository_providers.dart,
    lib/core/providers/stream_providers.dart,
    lib/core/router/app_router.dart,
    lib/presentation/screens/home_screen.dart,
    lib/presentation/screens/history_screen.dart,
    lib/presentation/screens/settings_screen.dart,
    lib/main.dart
  </files>
  <read_first>
    lib/data/database/app_database.dart (verify AppDatabase constructor signature and DAO accessors)
    lib/data/database/daos/water_entry_dao.dart (verify method signatures for watchEntriesForDate, watchTotalForDate, insertEntry, deleteLastEntry)
    lib/data/database/daos/user_settings_dao.dart (verify watchSettings, getSettings, updateSettings signatures)
    lib/data/database/daos/drink_preset_dao.dart (verify watchAllPresets, updatePreset signatures)
    lib/domain/entities/water_entry_entity.dart (verify WaterEntryEntity factory constructor fields)
    lib/domain/entities/user_settings_entity.dart (verify UserSettingsEntity factory constructor fields)
    lib/domain/entities/drink_preset_entity.dart (verify DrinkPresetEntity factory constructor fields)
    .planning/phases/01-data-foundation/01-RESEARCH.md (Patterns 6-8 for repository, provider, and GoRouter syntax; anti-patterns section for keepAlive and ref.watch rules)
  </read_first>
  <action>
    Create water_repository.dart per Pattern 6: class WaterRepository taking AppDatabase in constructor. Methods:
    - watchEntriesForDate(String dateKey) returning Stream of List of WaterEntryEntity, mapping Drift WaterEntry rows to WaterEntryEntity via named constructor fields (id, amountMl, loggedAt, dateKey)
    - insertEntry(int amountMl, DateTime loggedAt, String dateKey) returning Future void, calling waterEntryDao.insertEntry with WaterEntriesCompanion.insert
    - deleteLastEntry() returning Future int, delegating to waterEntryDao.deleteLastEntry
    - watchTotalForDate(String dateKey) returning Stream int, delegating to waterEntryDao.watchTotalForDate

    Create settings_repository.dart: class SettingsRepository taking AppDatabase in constructor. Methods:
    - watchSettings() returning Stream of UserSettingsEntity, mapping Drift UserSetting row to UserSettingsEntity via named constructor fields
    - getSettings() returning Future of UserSettingsEntity, with same mapping
    - updateSettings(UserSettingsEntity entity) returning Future void, converting entity fields to UserSettingsCompanion and calling userSettingsDao.updateSettings
    - watchPresets() returning Stream of List of DrinkPresetEntity, mapping Drift DrinkPreset rows to DrinkPresetEntity
    - updatePreset(int id, int amountMl) returning Future void, delegating to drinkPresetDao.updatePreset

    Create Riverpod provider files per Pattern 7, all with part directives for .g.dart:

    database_provider.dart: @Riverpod(keepAlive: true) function appDatabase taking Ref, creates AppDatabase(), registers ref.onDispose to close DB. Returns AppDatabase.

    repository_providers.dart: Two @Riverpod(keepAlive: true) functions -- waterRepository and settingsRepository, each taking Ref, calling ref.watch(appDatabaseProvider) to get DB, returning new WaterRepository(db) and SettingsRepository(db) respectively.

    stream_providers.dart: Four @Riverpod(keepAlive: true) functions:
    - todayWaterEntries(Ref ref) returning Stream of List of WaterEntryEntity, gets waterRepository from ref.watch, calls watchEntriesForDate with _todayDateKey()
    - todayTotalMl(Ref ref) returning Stream of int, gets waterRepository, calls watchTotalForDate with _todayDateKey()
    - userSettings(Ref ref) returning Stream of UserSettingsEntity, gets settingsRepository, calls watchSettings()
    - drinkPresets(Ref ref) returning Stream of List of DrinkPresetEntity, gets settingsRepository, calls watchPresets()
    Include private helper function _todayDateKey() that formats DateTime.now() as YYYY-MM-DD string with zero-padded month and day.

    Create app_router.dart per Pattern 8 and D-11: top-level GoRouter variable appRouter with initialLocation '/' and three GoRoute entries for paths /, /history, /settings building HomeScreen, HistoryScreen, SettingsScreen respectively.

    Create three placeholder screen files in lib/presentation/screens/:
    - home_screen.dart: StatelessWidget HomeScreen showing Scaffold with AppBar title "Drinky Drinky" and a Center body with Text "Home Screen". This is a placeholder for Phase 2.
    - history_screen.dart: StatelessWidget HistoryScreen with AppBar title "History" and Center Text "History Screen". Placeholder for Phase 4.
    - settings_screen.dart: StatelessWidget SettingsScreen with AppBar title "Settings" and Center Text "Settings Screen". Placeholder for Phase 3.

    Replace lib/main.dart per Pattern 8: WidgetsFlutterBinding.ensureInitialized() in main(), runApp wrapping ProviderScope around DrinkyDrinkyApp StatelessWidget, which returns MaterialApp.router with title 'Drinky Drinky', ThemeData using colorSchemeSeed Colors.blue and useMaterial3 true, routerConfig set to appRouter.

    Run dart run build_runner build --delete-conflicting-outputs to generate provider .g.dart files.

    Run flutter analyze to confirm zero errors (warnings are acceptable).
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && flutter analyze 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - water_repository.dart imports and maps Drift WaterEntry rows to WaterEntryEntity domain objects, not passing Drift types upstream
    - settings_repository.dart maps Drift UserSetting rows to UserSettingsEntity and DrinkPreset rows to DrinkPresetEntity
    - database_provider.dart uses @Riverpod(keepAlive: true) and registers ref.onDispose for db.close()
    - repository_providers.dart uses @Riverpod(keepAlive: true) for both waterRepository and settingsRepository providers
    - stream_providers.dart uses @Riverpod(keepAlive: true) for all four stream providers (todayWaterEntries, todayTotalMl, userSettings, drinkPresets)
    - stream_providers.dart contains _todayDateKey() formatting DateTime.now() as YYYY-MM-DD
    - app_router.dart defines GoRouter with three routes: / (HomeScreen), /history (HistoryScreen), /settings (SettingsScreen)
    - main.dart wraps app in ProviderScope and uses MaterialApp.router with appRouter
    - flutter analyze reports 0 errors (warnings acceptable)
    - All .g.dart files for providers are generated by build_runner
  </acceptance_criteria>
  <done>Repositories transform Drift rows to Freezed domain entities (per D-09, D-10). Riverpod providers with keepAlive: true expose reactive streams. GoRouter serves three routes per D-11. main.dart launches with ProviderScope. flutter analyze passes with 0 errors. The full data flow path is wired: AppDatabase -> DAOs -> Repositories -> Providers -> Screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Repository | amountMl and dateKey values enter the system through repository methods |
| Repository -> Drift DAO | Repositories pass user values into Drift Companion insert methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | WaterRepository.insertEntry | mitigate | Validate amountMl > 0 in repository before DAO insert; Drift type-safe API prevents SQL injection |
| T-01-02 | Tampering | dateKey format | mitigate | Repository validates dateKey matches YYYY-MM-DD regex before passing to DAO |
| T-01-03 | Information Disclosure | SQLite database file | accept | Hydration data is low-sensitivity; acceptable risk for v1; SQLCipher available if needed later |
| T-01-04 | Tampering | Drift queries | mitigate | Use only type-safe Drift API; never pass user input to customStatement |
| T-01-SC | Tampering | pub.dev installs | mitigate | All 15 packages verified via pub.dev with verified publishers in Package Legitimacy Audit |
</threat_model>

<verification>
1. build.yaml contains store_date_time_values_as_text: true
2. flutter pub get succeeds with 0 errors
3. dart run build_runner build --delete-conflicting-outputs succeeds with 0 errors
4. flutter analyze reports 0 errors
5. All .g.dart and .freezed.dart files are generated
6. AppDatabase.schemaVersion == 1
7. MigrationStrategy.onCreate seeds defaults: 2000ml target, 4 presets (200/300/400/500), DND 23:00-07:00
</verification>

<success_criteria>
- Flutter project with com.bizzarri.drinkydrinky bundle ID exists and compiles
- Drift database defines 3 tables (water_entries, user_settings, drink_presets) with correct schemas
- DAOs provide CRUD operations and reactive .watch() streams
- Freezed domain entities (WaterEntryEntity, DailyProgress, UserSettingsEntity, DrinkPresetEntity) generate successfully
- Repositories map Drift rows to domain entities and expose Stream-based APIs
- Riverpod providers with keepAlive: true wire DB through repositories to screens
- GoRouter serves 3 routes with placeholder screens
- main.dart launches with ProviderScope and MaterialApp.router
- flutter analyze passes with 0 errors
</success_criteria>

## Artifacts This Phase Produces

### Files Created
- build.yaml -- Drift code generation config
- lib/data/database/tables/water_entries_table.dart -- WaterEntries table class
- lib/data/database/tables/user_settings_table.dart -- UserSettings table class
- lib/data/database/tables/drink_presets_table.dart -- DrinkPresets table class
- lib/data/database/app_database.dart -- AppDatabase class with migration seeding
- lib/data/database/daos/water_entry_dao.dart -- WaterEntryDao with CRUD + streams
- lib/data/database/daos/user_settings_dao.dart -- UserSettingsDao with single-row pattern
- lib/data/database/daos/drink_preset_dao.dart -- DrinkPresetDao with ordered queries
- lib/domain/entities/water_entry_entity.dart -- WaterEntryEntity Freezed class
- lib/domain/entities/daily_progress.dart -- DailyProgress Freezed class
- lib/domain/entities/user_settings_entity.dart -- UserSettingsEntity Freezed class
- lib/domain/entities/drink_preset_entity.dart -- DrinkPresetEntity Freezed class
- lib/data/repositories/water_repository.dart -- WaterRepository (Drift -> domain mapping)
- lib/data/repositories/settings_repository.dart -- SettingsRepository (settings + presets)
- lib/core/providers/database_provider.dart -- appDatabaseProvider
- lib/core/providers/repository_providers.dart -- waterRepositoryProvider, settingsRepositoryProvider
- lib/core/providers/stream_providers.dart -- todayWaterEntriesProvider, todayTotalMlProvider, userSettingsProvider, drinkPresetsProvider
- lib/core/router/app_router.dart -- GoRouter with 3 routes
- lib/presentation/screens/home_screen.dart -- Placeholder HomeScreen
- lib/presentation/screens/history_screen.dart -- Placeholder HistoryScreen
- lib/presentation/screens/settings_screen.dart -- Placeholder SettingsScreen
- lib/main.dart -- App entry point

### Key Symbols Exported
- AppDatabase (constructor accepts optional QueryExecutor for testing)
- WaterEntryDao, UserSettingsDao, DrinkPresetDao
- WaterEntryEntity, DailyProgress, UserSettingsEntity, DrinkPresetEntity
- WaterRepository, SettingsRepository
- appDatabaseProvider, waterRepositoryProvider, settingsRepositoryProvider
- todayWaterEntriesProvider, todayTotalMlProvider, userSettingsProvider, drinkPresetsProvider
- appRouter (GoRouter instance)

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done
</output>
