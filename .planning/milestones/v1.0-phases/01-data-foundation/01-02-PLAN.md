---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/data/database/daos/water_entry_dao_test.dart
  - test/data/database/daos/user_settings_dao_test.dart
  - test/data/database/daos/drink_preset_dao_test.dart
autonomous: false
requirements: []

must_haves:
  truths:
    - "Unit tests pass against an in-memory database covering all DAO operations"
    - "Water entry insert, query-by-date, total aggregation, and delete-last all work correctly"
    - "Default settings and presets are seeded on database creation"
    - "Settings updates persist and are retrievable"
    - "The app launches on a simulator/device and displays the home screen"
  artifacts:
    - path: "test/data/database/daos/water_entry_dao_test.dart"
      provides: "WaterEntryDao test coverage"
      contains: "watchEntriesForDate"
    - path: "test/data/database/daos/user_settings_dao_test.dart"
      provides: "UserSettingsDao test coverage"
      contains: "getSettings"
    - path: "test/data/database/daos/drink_preset_dao_test.dart"
      provides: "DrinkPresetDao test coverage"
      contains: "watchAllPresets"
  key_links:
    - from: "test/data/database/daos/water_entry_dao_test.dart"
      to: "lib/data/database/app_database.dart"
      via: "AppDatabase(DatabaseConnection(NativeDatabase.memory()))"
      pattern: "NativeDatabase\\.memory\\(\\)"
---

<objective>
Write unit tests for all three DAOs using Drift's in-memory database per D-16, then verify the full app launches on a device or simulator. This proves the walking skeleton works end-to-end: database initializes, seeds defaults, handles CRUD operations correctly, and the app runs with the complete data flow wired.

Purpose: Validate that the data foundation is correct before any subsequent phase builds on it. Tests catch schema errors, seed data issues, and DAO logic bugs in isolation. The human-verify checkpoint confirms the full stack runs on a real device.

Output: Three test files covering all DAO operations, plus confirmed app launch.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for all DAOs using in-memory Drift database</name>
  <files>
    test/data/database/daos/water_entry_dao_test.dart,
    test/data/database/daos/user_settings_dao_test.dart,
    test/data/database/daos/drink_preset_dao_test.dart
  </files>
  <read_first>
    lib/data/database/app_database.dart (verify constructor signature, DAO accessor names, generated import paths)
    lib/data/database/daos/water_entry_dao.dart (verify method signatures: insertEntry, watchEntriesForDate, watchTotalForDate, deleteLastEntry, watchEntriesInRange)
    lib/data/database/daos/user_settings_dao.dart (verify method signatures: watchSettings, getSettings, updateSettings)
    lib/data/database/daos/drink_preset_dao.dart (verify method signatures: watchAllPresets, updatePreset)
    lib/data/database/app_database.g.dart (verify generated Companion classes: WaterEntriesCompanion.insert parameters, UserSettingsCompanion fields)
    .planning/phases/01-data-foundation/01-RESEARCH.md (Testing Approach section for NativeDatabase.memory() setup pattern, closeStreamsSynchronously: true requirement)
  </read_first>
  <behavior>
    WaterEntryDao tests:
    - Test: insert an entry with amountMl=250, dateKey='2026-06-03', then watchEntriesForDate('2026-06-03').first returns list of length 1 with amountMl==250
    - Test: insert two entries for same date with amountMl 200 and 300, watchTotalForDate returns 500
    - Test: insert two entries, deleteLastEntry removes the one with later loggedAt, remaining entry is the earlier one
    - Test: watchEntriesForDate returns entries ordered by loggedAt ascending
    - Test: entries for different date_keys are isolated (insert for '2026-06-03' does not appear in watchEntriesForDate('2026-06-04'))
    - Test: watchEntriesInRange returns entries within the date range only

    UserSettingsDao tests:
    - Test: after database creation, getSettings returns defaults: dailyTargetMl==2000, notificationIntervalMinutes==60, dndStartHour==23, dndStartMinute==0, dndEndHour==7, dndEndMinute==0, dndEnabled==true
    - Test: updateSettings with dailyTargetMl=2500 then getSettings returns 2500
    - Test: watchSettings emits updated value after updateSettings is called

    DrinkPresetDao tests:
    - Test: after database creation, watchAllPresets.first returns 4 presets with amountMl [200, 300, 400, 500] in sortOrder
    - Test: updatePreset changes a preset's amountMl and watchAllPresets reflects the change
  </behavior>
  <action>
    Create the test directory structure: test/data/database/daos/

    Create water_entry_dao_test.dart following the test pattern from RESEARCH.md Testing Approach section. Import package:drift/drift.dart, package:drift/native.dart, package:flutter_test/flutter_test.dart, and the app's AppDatabase. In setUp(), create AppDatabase with DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true) -- the closeStreamsSynchronously parameter prevents "timer after test" errors. In tearDown(), call db.close().

    Write a group named 'WaterEntryDao' containing all tests from the behavior section above. For each insert, use WaterEntriesCompanion.insert with the required parameters (amountMl, loggedAt as DateTime, dateKey as String). Use .first on stream results to get a single emission.

    Create user_settings_dao_test.dart with the same setUp/tearDown pattern. Write a group named 'UserSettingsDao' with tests verifying default seed values match D-05/D-07/D-08, settings update works, and watchSettings emits reactively.

    Create drink_preset_dao_test.dart with the same pattern. Write a group named 'DrinkPresetDao' with tests verifying default preset seed values match D-06 (200/300/400/500ml) in correct sort order, and preset updates work.

    Run flutter test to verify all tests pass.
  </action>
  <verify>
    <automated>flutter test test/data/database/daos/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist under test/data/database/daos/
    - Each test file creates AppDatabase with NativeDatabase.memory() and closeStreamsSynchronously: true
    - Each test file calls db.close() in tearDown
    - water_entry_dao_test.dart has at least 5 tests covering insert, query-by-date, total aggregation, delete-last, date isolation
    - user_settings_dao_test.dart has at least 3 tests covering default seed verification (2000ml, 60min, DND 23:00-07:00, enabled), update, and stream reactivity
    - drink_preset_dao_test.dart has at least 2 tests covering default presets (200/300/400/500ml in order) and update
    - flutter test completes with 0 failures
  </acceptance_criteria>
  <done>All DAO unit tests pass against in-memory Drift database per D-16. Tests verify: water entry CRUD and date isolation, default settings seed values (D-05/D-07/D-08), default preset seed values (D-06), and reactive stream behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify app launches and full stack works</name>
  <files>none</files>
  <action>
    This is a human verification checkpoint. Claude has automated all implementation work in prior tasks. The user now verifies the walking skeleton works end-to-end on a real device or simulator.
  </action>
  <what-built>Complete data foundation: Flutter project with Drift database (3 tables with seeded defaults), DAOs with reactive streams, Freezed domain entities, repositories, Riverpod providers, GoRouter with 3 routes, and all unit tests passing.</what-built>
  <how-to-verify>
    1. Run flutter test test/data/database/daos/ -- confirm all tests pass with 0 failures
    2. Run flutter run on an iOS simulator or Android emulator
    3. Confirm the app launches and shows "Drinky Drinky" in the AppBar with "Home Screen" text centered on screen
    4. Check the terminal/console for any Drift database initialization errors (there should be none)
    5. The home screen should appear within a few seconds with no crashes
  </how-to-verify>
  <verify>
    <automated>flutter test test/data/database/daos/ 2>&1 | tail -5</automated>
    <human-check>App launches on simulator/emulator showing "Drinky Drinky" AppBar and "Home Screen" text with no console errors</human-check>
  </verify>
  <done>User confirms app launches without crashes, home screen displays correctly, and all DAO unit tests pass.</done>
  <resume-signal>Type "approved" if the app launches cleanly and tests pass, or describe any issues you see</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data -> in-memory DB | Test data is controlled; no trust boundary concerns in test context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Test database | accept | In-memory database is ephemeral; no persistence risk |
| T-01-SC | Tampering | pub.dev installs | mitigate | All packages verified in Plan 01 Package Legitimacy Audit |
</threat_model>

<verification>
1. flutter test test/data/database/daos/ passes with 0 failures
2. flutter run launches the app without crashes
3. No Drift database initialization errors in console
4. Home screen displays "Drinky Drinky" AppBar title
</verification>

<success_criteria>
- All DAO unit tests pass (water entries: insert/query/total/delete/date-isolation, settings: defaults/update/stream, presets: defaults/update)
- App launches on simulator/emulator without crashes
- Drift database initializes successfully with seeded defaults
- The walking skeleton is proven: real DB read/write works, reactive streams connected, UI renders
</success_criteria>

## Artifacts This Phase Produces

### Files Created
- test/data/database/daos/water_entry_dao_test.dart -- WaterEntryDao unit tests
- test/data/database/daos/user_settings_dao_test.dart -- UserSettingsDao unit tests
- test/data/database/daos/drink_preset_dao_test.dart -- DrinkPresetDao unit tests

### Key Symbols Exported
- None (test files only)

<output>
Create `.planning/phases/01-data-foundation/01-02-SUMMARY.md` when done
</output>
