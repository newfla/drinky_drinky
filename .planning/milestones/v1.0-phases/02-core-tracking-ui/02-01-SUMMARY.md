---
phase: 02-core-tracking-ui
plan: 01
subsystem: ui
tags: [flutter, go_router, navigation, percent_indicator, riverpod, material3]

# Dependency graph
requires:
  - phase: 01-data-foundation
    provides: GoRouter provider (appRouter) that this plan migrates to shell route

provides:
  - StatefulShellRoute.indexedStack navigation shell with bottom NavigationBar (3 tabs)
  - percent_indicator 4.2.5 dependency available for import
  - HistoryScreen stub with "Coming soon" and "History" AppBar
  - SettingsScreen stub with "Coming soon" and "Settings" AppBar

affects: [02-02-home-screen, 03-settings, 04-calendar-streaks]

# Tech tracking
tech-stack:
  added: [percent_indicator 4.2.5]
  patterns:
    - StatefulShellRoute.indexedStack with NavigationBar shell builder (M3 bottom nav)
    - Per-branch Scaffold pattern (shell Scaffold provides NavigationBar; branch Scaffold provides AppBar)

key-files:
  created: []
  modified:
    - pubspec.yaml
    - lib/core/router/app_router.dart
    - lib/core/router/app_router.g.dart
    - lib/presentation/screens/history_screen.dart
    - lib/presentation/screens/settings_screen.dart

key-decisions:
  - "Router migrated from flat GoRoute list to StatefulShellRoute.indexedStack per D-06/D-07; initialLocation stays '/'"
  - "Each branch screen retains its own Scaffold + AppBar (nested Scaffold pattern); shell Scaffold provides NavigationBar only"
  - "History and Settings are real NavigationBar destinations showing 'Coming soon' stubs per D-07"
  - "percent_indicator ^4.2.5 added to pubspec.yaml (approved in CLAUDE.md tech stack)"

patterns-established:
  - "Pattern: StatefulShellRoute shell builder wraps Scaffold(body: navigationShell, bottomNavigationBar: NavigationBar(...))"
  - "Pattern: Branch screens use their own Scaffold with AppBar; not the shell Scaffold"

requirements-completed: [HOME-01, HOME-02, HOME-03, HOME-04]

# Metrics
duration: 2min
completed: 2026-06-04
---

# Phase 2 Plan 1: Navigation Shell Summary

**StatefulShellRoute.indexedStack with M3 NavigationBar (Home/History/Settings), percent_indicator 4.2.5 added, stub screens with 'Coming soon' content per D-07**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-04T14:48:23Z
- **Completed:** 2026-06-04T14:50:41Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Migrated GoRouter from flat GoRoute list to StatefulShellRoute.indexedStack with NavigationBar shell, providing persistent tab state across Home, History, and Settings tabs
- Added percent_indicator 4.2.5 to pubspec.yaml and ran flutter pub get successfully (prerequisite for Plan 02-02 progress ring widget)
- Updated HistoryScreen and SettingsScreen body text to "Coming soon" per D-07; both retain StatelessWidget, Scaffold, and correct AppBar titles
- dart analyze reports no issues; iOS debug build succeeds (flutter build ios --debug --no-codesign)
- Regenerated app_router.g.dart via build_runner after router rewrite

## Task Commits

Each task was committed atomically:

1. **Task 1: Add percent_indicator and migrate router to StatefulShellRoute.indexedStack** - `d35b261` (feat)
2. **Task 2: Update History and Settings screens to "Coming soon" stubs** - `db70830` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified

- `pubspec.yaml` — added `percent_indicator: ^4.2.5` dependency
- `lib/core/router/app_router.dart` — rewrote router internals: StatefulShellRoute.indexedStack, NavigationBar shell builder with 3 NavigationDestination widgets, 3 StatefulShellBranch entries for /, /history, /settings
- `lib/core/router/app_router.g.dart` — regenerated by build_runner (no manual changes)
- `lib/presentation/screens/history_screen.dart` — body text changed from 'History Screen' to 'Coming soon'
- `lib/presentation/screens/settings_screen.dart` — body text changed from 'Settings Screen' to 'Coming soon'

## Decisions Made

- Used M3 NavigationBar (not deprecated BottomNavigationBar) consistent with `useMaterial3: true` in main.dart
- Shell builder Scaffold provides only the NavigationBar; each branch screen keeps its own Scaffold + AppBar (standard GoRouter shell pattern per RESEARCH.md Pattern 1)
- NavigationDestination icons: `Icons.water_drop_outlined`/`Icons.water_drop` for Home, `Icons.calendar_month_outlined`/`Icons.calendar_month` for History, `Icons.settings_outlined`/`Icons.settings` for Settings — exactly as specified in UI-SPEC
- `initialLocation: '/'` preserved as required by D-06 (always start on Home tab)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

- Android debug build (`flutter build apk --debug`) failed during Gradle distribution download due to an SSL certificate error (PKIX path building failed). This is a local Java/network configuration issue — the Gradle wrapper cannot download its distribution over HTTPS. The error is unrelated to this plan's code changes. iOS debug build succeeded. `dart analyze` reports no errors. This issue is pre-existing in the environment and deferred to `deferred-items.md`.

## Known Stubs

The "Coming soon" stubs in HistoryScreen and SettingsScreen are intentional per D-07. They are real NavigationBar destinations with placeholder content:
- `lib/presentation/screens/history_screen.dart` — "Coming soon" body; replaced in Phase 4 (Calendar & Streaks)
- `lib/presentation/screens/settings_screen.dart` — "Coming soon" body; replaced in Phase 3 (Settings)

These stubs do not prevent this plan's goal from being achieved — providing the navigation shell with working tab routing is the explicit deliverable.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Navigation shell complete; Plan 02-02 can now build the full HomeScreen within the established shell context
- percent_indicator is installed and ready for use in the progress ring widget
- ScaffoldMessenger context is correctly scoped: each branch screen has its own Scaffold, so SnackBars from HomeScreen will find the HomeScreen's Scaffold
- All HOME-01 through HOME-04 requirements are scaffolded via the navigation container; Plan 02-02 implements the actual home screen content

---
*Phase: 02-core-tracking-ui*
*Completed: 2026-06-04*
