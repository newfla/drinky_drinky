---
phase: 04-calendar-streaks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/data/database/daos/water_entry_dao.dart
  - lib/data/repositories/water_repository.dart
  - lib/core/providers/stream_providers.dart
  - lib/core/providers/stream_providers.g.dart
  - lib/presentation/screens/history_screen.dart
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "User can view a monthly calendar where each past day is colored green (goal met) or red (goal missed)"
    - "User can navigate between months to review historical hydration data"
    - "User can see their current streak of consecutive days with the daily goal reached"
    - "Future months are blocked (cannot navigate past current month)"
    - "Tapping a day shows a summary card with that day's total intake"
    - "Switching tabs and returning preserves the selected month"
  artifacts:
    - path: "lib/data/database/daos/water_entry_dao.dart"
      provides: "getEarliestDateKey() DAO method"
      contains: "getEarliestDateKey"
    - path: "lib/data/repositories/water_repository.dart"
      provides: "watchDailyTotalsInRange() and getEarliestDateKey() repository methods"
      contains: "watchDailyTotalsInRange"
    - path: "lib/core/providers/stream_providers.dart"
      provides: "calendarMonthProvider, streakProvider, focusedMonthProvider"
      exports: ["calendarMonthProvider", "streakProvider", "focusedMonthProvider"]
    - path: "lib/presentation/screens/history_screen.dart"
      provides: "Full HistoryScreen with StreakCard and TableCalendar"
      min_lines: 150
    - path: "pubspec.yaml"
      provides: "table_calendar dependency"
      contains: "table_calendar"
  key_links:
    - from: "lib/core/providers/stream_providers.dart"
      to: "lib/data/repositories/water_repository.dart"
      via: "calendarMonthProvider and streakProvider watch waterRepositoryProvider"
      pattern: "ref\\.watch\\(waterRepositoryProvider\\)"
    - from: "lib/presentation/screens/history_screen.dart"
      to: "lib/core/providers/stream_providers.dart"
      via: "ref.watch on calendarMonthProvider, streakProvider, focusedMonthProvider, userSettingsProvider"
      pattern: "ref\\.watch\\(calendarMonthProvider"
    - from: "lib/data/repositories/water_repository.dart"
      to: "lib/data/database/daos/water_entry_dao.dart"
      via: "watchDailyTotalsInRange calls watchEntriesInRange, getEarliestDateKey pass-through"
      pattern: "waterEntryDao\\.watchEntriesInRange"
---

<objective>
Implement the full Calendar and Streaks feature for Phase 4, replacing the HistoryScreen stub with a functional monthly calendar (table_calendar 3.2.0) showing green/red day decorations based on goal attainment, a streak counter card, and a day summary on tap. Covers HIST-01 (calendar with green/red days, D-01 through D-05, D-10, D-11) and HIST-02 (streak counter, D-04, D-06 through D-09, D-12).

Purpose: Users can review their hydration history and see how many consecutive days they have met their goal -- the last read-only feature before notifications.
Output: Working history screen with calendar, streak card, and day summary accessible via the existing History tab.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-calendar-streaks/04-CONTEXT.md
@.planning/phases/04-calendar-streaks/04-RESEARCH.md
@.planning/phases/04-calendar-streaks/04-PATTERNS.md
@.planning/phases/04-calendar-streaks/04-UI-SPEC.md
@lib/data/database/daos/water_entry_dao.dart
@lib/data/repositories/water_repository.dart
@lib/core/providers/stream_providers.dart
@lib/core/providers/repository_providers.dart
@lib/presentation/screens/history_screen.dart
@lib/presentation/screens/home_screen.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer -- DAO method, repository methods, and table_calendar dependency</name>
  <files>
    pubspec.yaml
    lib/data/database/daos/water_entry_dao.dart
    lib/data/repositories/water_repository.dart
  </files>
  <action>
    1. Add `table_calendar: ^3.2.0` under the `# UI Components` section in pubspec.yaml (after percent_indicator). Run `fvm flutter pub get` to resolve.

    2. In `water_entry_dao.dart`, add `getEarliestDateKey()` method per D-05. Follow the existing `deleteLastEntry` query pattern: `select(waterEntries)` with `orderBy([(t) => OrderingTerm.asc(t.dateKey)])` and `limit(1)`, then `getSingleOrNull()`. Return `result?.dateKey` as `Future<String?>`. Place it after the existing `deleteLastEntry` method.

    3. In `water_repository.dart`, add two methods per D-01 and D-05:

       a. `watchDailyTotalsInRange(String startDateKey, String endDateKey)` returning `Stream<Map<String, int>>`. Import `package:collection/collection.dart` (transitive dep, already in pubspec.lock). Call `_db.waterEntryDao.watchEntriesInRange(startDateKey, endDateKey)` and `.map()` the stream: use `groupBy(entries, (e) => e.dateKey)` then `.map((dateKey, dayEntries) => MapEntry(dateKey, dayEntries.fold(0, (sum, e) => sum + e.amountMl)))`. Days with zero entries are absent from the map (D-01). This single method serves both calendar and streak per D-02.

       b. `getEarliestDateKey()` returning `Future<String?>` as a pass-through to `_db.waterEntryDao.getEarliestDateKey()` per D-05. Follow the existing `deleteLastEntry` pass-through pattern.

    Do NOT add `collection` to pubspec.yaml explicitly -- it is a transitive dependency from Flutter SDK and is safe to import directly (per RESEARCH.md open question 1 recommendation).
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && fvm flutter pub get && grep -c 'table_calendar' pubspec.yaml && grep -c 'getEarliestDateKey' lib/data/database/daos/water_entry_dao.dart && grep -c 'watchDailyTotalsInRange' lib/data/repositories/water_repository.dart && grep -c 'getEarliestDateKey' lib/data/repositories/water_repository.dart</automated>
  </verify>
  <done>pubspec.yaml includes table_calendar ^3.2.0 and pub get succeeds. water_entry_dao.dart has getEarliestDateKey() method. water_repository.dart has watchDailyTotalsInRange() and getEarliestDateKey() methods. Both follow existing codebase patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Riverpod providers -- calendarMonthProvider, streakProvider, focusedMonthProvider</name>
  <files>
    lib/core/providers/stream_providers.dart
    lib/core/providers/stream_providers.g.dart
  </files>
  <action>
    Add three new providers to `stream_providers.dart` per D-06 through D-09. Follow existing provider patterns in the file (imports, annotation style, todayDateKey helper).

    1. `calendarMonthProvider(Ref ref, int year, int month)` -- per D-06 and D-07. Annotate with `@riverpod` (auto-dispose is fine per Claude's discretion). Returns `Stream<Map<String, int>>`. Compute `firstDay = DateTime(year, month, 1)` and `lastDay = DateTime(year, month + 1, 0)` (Dart handles month overflow). Convert both to dateKey strings using the same format as `todayDateKey()`. Call `repo.watchDailyTotalsInRange(startKey, endKey)` per D-03. Get repo via `ref.watch(waterRepositoryProvider)`.

    2. `streakProvider` -- per D-06 and D-08. Annotate with `@riverpod` (auto-dispose fine). Returns `Stream<int>`. Use `async*` generator pattern. Watch `waterRepositoryProvider` and `userSettingsProvider`. Guard: if settings is null or `dailyTargetMl <= 0`, yield 0 and return (edge case per RESEARCH.md Pitfall 6). Compute `yesterday = DateTime.now().subtract(Duration(days: 1))`. Call `repo.watchDailyTotalsInRange('2020-01-01', yesterdayKey)` per D-08 (veryEarlyDate fallback). Use `yield*` to forward the mapped stream: walk backward from yesterday, incrementing streak count for each consecutive day where `totals[dateKey] != null && totals[dateKey]! >= dailyTarget`, breaking on first miss. Create a local `_toDateKey(DateTime d)` helper function (same format as todayDateKey) for converting arbitrary dates to dateKey strings.

    3. `FocusedMonth` Notifier -- per D-09. Annotate with `@Riverpod(keepAlive: true)` class pattern (NotifierProvider, per Claude's discretion -- consistent with code-gen convention, no raw StateProvider in codebase). `build()` returns `DateTime.now()`. Single `set(DateTime month)` method that assigns `state = month`. This survives tab switches because keepAlive prevents disposal.

    After adding all three providers, run `dart run build_runner build --delete-conflicting-outputs` to generate the `.g.dart` file with the new provider bindings. Verify the build completes without errors.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && grep -c 'calendarMonth' lib/core/providers/stream_providers.dart && grep -c 'streakProvider\|streak(' lib/core/providers/stream_providers.dart && grep -c 'FocusedMonth' lib/core/providers/stream_providers.dart</automated>
  </verify>
  <done>stream_providers.dart contains calendarMonthProvider (family, @riverpod), streakProvider (@riverpod, async*), and FocusedMonth Notifier (@Riverpod(keepAlive: true)). build_runner generates stream_providers.g.dart without errors. All three providers follow existing code-gen conventions.</done>
</task>

<task type="auto">
  <name>Task 3: HistoryScreen UI -- StreakCard, TableCalendar, DaySummary</name>
  <files>
    lib/presentation/screens/history_screen.dart
  </files>
  <action>
    Replace the entire `history_screen.dart` stub with the full implementation per D-12 and 04-UI-SPEC.md. Follow the ConsumerStatefulWidget pattern from home_screen.dart.

    **Imports:** flutter/material.dart, flutter_riverpod, table_calendar (for TableCalendar, CalendarFormat, CalendarBuilders, HeaderStyle, StartingDayOfWeek, isSameDay), stream_providers.dart, repository_providers.dart.

    **Class structure:** `HistoryScreen extends ConsumerStatefulWidget`. State class `_HistoryScreenState` has:
    - `DateTime? _firstDay` (nullable until getEarliestDateKey resolves) per D-11
    - `DateTime? _selectedDay` (local state for day tap)
    - `bool _loading = true` (controls initial spinner)

    **initState per D-12:** Call `ref.read(waterRepositoryProvider).getEarliestDateKey()` (per D-11). On resolve: if result is non-null, parse it to DateTime and set `_firstDay`; if null, fall back to `DateTime(2020, 1, 1)`. Set `_loading = false`. Use `if (mounted) setState(...)` guard. Do NOT use `ref.watch` in initState -- use `ref.read` for the one-time Future.

    **build method:**
    - If `_loading` is true: return `Scaffold(appBar: AppBar(title: Text('History')), body: Center(child: CircularProgressIndicator()))` per D-11.
    - Otherwise: watch `focusedMonthProvider` for the current month DateTime. Watch `userSettingsProvider` for dailyTargetMl. Use `settingsAsync.when(loading/error/data)` pattern from home_screen.dart. Error copy: "Something went wrong loading your data." per UI-SPEC.
    - Inside the data branch: watch `calendarMonthProvider(focused.year, focused.month)` for the month's totals map. Watch `streakProvider` for the streak int. Both use `.value ?? defaultValue` pattern.

    **Layout per UI-SPEC Component Inventory:**
    Scaffold body is a `SingleChildScrollView` containing a `Column`:

    1. **SizedBox(height: 24)** -- lg top padding
    2. **StreakCard** -- `Card` with `margin: EdgeInsets.symmetric(horizontal: 16)`, internal `Padding(EdgeInsets.all(16))`, containing a `Row(crossAxisAlignment: CrossAxisAlignment.center)`:
       - `Icon(Icons.local_fire_department, size: 32, color: Colors.orange.shade700)`
       - `SizedBox(width: 8)`
       - `Text('$streak', style: theme.textTheme.titleLarge?.copyWith(fontWeight: FontWeight.w600))`
       - `SizedBox(width: 4)`
       - `Text(' day streak', style: theme.textTheme.bodyLarge?.copyWith(color: colorScheme.onSurfaceVariant))`
    3. **SizedBox(height: 24)** -- lg spacing
    4. **TableCalendar** configured per UI-SPEC and D-10/D-11:
       - `firstDay: _firstDay!` (guaranteed non-null after loading)
       - `lastDay: DateTime(DateTime.now().year, DateTime.now().month + 1, 0)` per D-10 (end of current month, blocks future)
       - `focusedDay: focused` (from focusedMonthProvider). **Clamp** focusedDay to [firstDay, lastDay] range before passing to prevent assertion error per RESEARCH.md Pitfall 3.
       - `calendarFormat: CalendarFormat.month` (fixed)
       - `availableCalendarFormats: const {CalendarFormat.month: 'Month'}` (hides format button)
       - `startingDayOfWeek: StartingDayOfWeek.monday`
       - `headerStyle: HeaderStyle(formatButtonVisible: false, titleCentered: true)`
       - `selectedDayPredicate: (day) => isSameDay(_selectedDay, day)` (from table_calendar)
       - `onDaySelected: (selectedDay, focusedDay)` -- set `_selectedDay = selectedDay` via setState. Only allow selection for days that are not after today.
       - `onPageChanged: (focusedDay)` -- call `ref.read(focusedMonthProvider.notifier).set(focusedDay)` per D-09. Extract only `.year` and `.month` for provider lookups per RESEARCH.md Pitfall 4.
       - `calendarBuilders: CalendarBuilders(defaultBuilder: ..., todayBuilder: ...)` per UI-SPEC CalendarBuilders Day Decoration Logic.

    5. **CalendarBuilders implementation** per UI-SPEC and RESEARCH.md Pattern 4:
       - Create a `_buildDayCell(BuildContext context, DateTime day, bool? metGoal, {required bool isToday})` helper method (must accept context for theme access).
       - `defaultBuilder`: compute dateKey from day using `_toDateKey(day)`. Look up `monthTotals[dateKey]`. If null (no data), return null (default rendering -- no decoration for days with zero entries per UI-SPEC rule 4). If `total >= dailyTarget`, pass `metGoal: true`. If `total > 0 && total < dailyTarget`, pass `metGoal: false`. Days after today: return null.
       - `todayBuilder`: same lookup logic. If no data, show today ring only (primary border, no fill). If data exists, show green/red fill AND today ring overlay per UI-SPEC rule 2.
       - Day cell widget per UI-SPEC: `Container` with `margin: EdgeInsets.all(4)`, `BoxDecoration(shape: BoxShape.circle, color: fillColor, border: isToday ? Border.all(color: colorScheme.primary, width: 2) : null)`, `alignment: Alignment.center`, child `Text('${day.day}', style: TextStyle(color: textColor, fontWeight: FontWeight.w500))`.
       - Green: `fillColor = Colors.green.shade600.withValues(alpha: 0.15)`, `textColor = Colors.green.shade600`.
       - Red: `fillColor = Colors.red.shade600.withValues(alpha: 0.15)`, `textColor = Colors.red.shade600`.
       - Use `withValues(alpha:)` NOT `withOpacity()` (deprecated in Flutter 3.44.1).

    6. **SizedBox(height: 16)** -- md spacing
    7. **DaySummaryArea** per UI-SPEC Day Summary section:
       - Wrap in `AnimatedSwitcher(duration: Duration(milliseconds: 200))`.
       - If `_selectedDay` is null, show `SizedBox.shrink()` (key it differently so AnimatedSwitcher animates).
       - If `_selectedDay` is set, show a `Card` with `margin: EdgeInsets.symmetric(horizontal: 16)`, `Padding(EdgeInsets.all(16))`, displaying: date label + total intake. Format: "{Month} {day}, {year} -- {total} of {target} ml" or "{Month} {day}, {year} -- No entries" per UI-SPEC copywriting contract.
       - For date formatting use manual month name lookup (avoid adding intl dependency per RESEARCH.md open question 2): create a `_monthName(int month)` helper returning "January" through "December".
       - Look up the selected day's total from the monthTotals map. If the selected day is in a different month than the focused month, the total may not be in the current map -- show "No entries" in that case.

    8. **Empty state** per UI-SPEC: If `_firstDay` resolved from getEarliestDateKey as null AND there are no entries at all, show the empty state: heading "No history yet", body "Start logging water on the Home tab to see your history here." centered in the body instead of the calendar. Use the same empty state pattern as home_screen.dart.

    9. **Accessibility per UI-SPEC:** Wrap each decorated day cell in a `Semantics` widget with `label: "{Month} {day}: goal met"` or `"{Month} {day}: goal not met"`. Streak card reads naturally as "{N} day streak".

    **Local helper:** Create `String _toDateKey(DateTime d)` as a file-private function, same format as `todayDateKey()` in stream_providers.dart: `'${d.year}-${d.month.toString().padLeft(2, '0')}-${d.day.toString().padLeft(2, '0')}'`.

    After completing the file, run `fvm flutter analyze` to verify no lint errors.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && fvm flutter analyze lib/presentation/screens/history_screen.dart 2>&1 | tail -5 && grep -c 'TableCalendar' lib/presentation/screens/history_screen.dart && grep -c 'StreakCard\|day streak\|local_fire_department' lib/presentation/screens/history_screen.dart && grep -c 'ConsumerStatefulWidget' lib/presentation/screens/history_screen.dart && grep -c 'AnimatedSwitcher' lib/presentation/screens/history_screen.dart</automated>
    <human-check>
      1. Open the app on iOS simulator or Android emulator
      2. Navigate to the History tab
      3. Verify the streak card shows "{N} day streak" with flame icon at the top
      4. Verify the calendar shows the current month with green/red day decorations for past days with data
      5. Swipe left/right to navigate months -- verify future months are blocked
      6. Tap a past day with data -- verify the summary card appears below with "{date} -- {total} of {target} ml"
      7. Switch to Home tab and back to History -- verify the selected month is preserved
      8. If no water entries exist yet, verify the empty state shows "No history yet"
    </human-check>
  </verify>
  <done>HistoryScreen is a ConsumerStatefulWidget showing StreakCard (flame icon + count + "day streak"), TableCalendar with green/red day cells (CircleShape, muted fill + bold text), today ring (primary border), month navigation (blocked past current month), day tap summary (AnimatedSwitcher Card with date + total), and empty state. All visual specs match 04-UI-SPEC.md. flutter analyze passes with no errors on history_screen.dart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase is a read-only view of locally stored data. No user input, no network calls, no destructive actions. All data originates from the existing validated WaterRepository.insertEntry (Phase 1 T-01-01/T-01-02 input validation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | history_screen.dart | accept | Data is local-only, single-user, no PII beyond water intake amounts. No network transmission. |
| T-04-02 | Denial of Service | streakProvider (full history query) | accept | Streak provider queries entire history on every table change (per D-04/D-08). Acceptable: table grows ~5-10 entries/day, computation is O(n) map lookup. No user-facing lag expected for a personal app. |
| T-04-SC | Tampering | pub.dev install (table_calendar) | mitigate | table_calendar verified via pub.dev direct fetch: 3.32k likes, 546k downloads, Apache-2.0 license, active maintenance. Package Legitimacy Audit in RESEARCH.md confirms Approved status. |
</threat_model>

<verification>
1. `fvm flutter analyze` passes with no errors across all modified files
2. `fvm flutter test` passes (existing tests unbroken)
3. `dart run build_runner build --delete-conflicting-outputs` completes without errors
4. `grep -c 'table_calendar' pubspec.yaml` returns at least 1
5. `grep -c 'getEarliestDateKey' lib/data/database/daos/water_entry_dao.dart` returns at least 1
6. `grep -c 'watchDailyTotalsInRange' lib/data/repositories/water_repository.dart` returns at least 1
7. `grep -c 'calendarMonthProvider' lib/core/providers/stream_providers.dart` returns at least 1
8. `grep -c 'streakProvider\|streak(' lib/core/providers/stream_providers.dart` returns at least 1
9. `grep -c 'FocusedMonth' lib/core/providers/stream_providers.dart` returns at least 1
10. `grep -c 'TableCalendar' lib/presentation/screens/history_screen.dart` returns at least 1
11. `grep -c 'ConsumerStatefulWidget' lib/presentation/screens/history_screen.dart` returns at least 1
</verification>

<success_criteria>
1. User can view a monthly calendar where each past day is colored green (goal met) or red (goal missed) -- HIST-01
2. User can navigate between months (swipe or arrow tap) and future months are blocked -- HIST-01, D-10
3. User can see their current streak of consecutive days with the daily goal reached -- HIST-02
4. Tapping a past day shows a summary card with date and total intake
5. Tab switching preserves the selected month -- D-09
6. Empty state shows "No history yet" when no entries exist
7. All existing tests pass, flutter analyze is clean
</success_criteria>

<output>
Create `.planning/phases/04-calendar-streaks/04-01-SUMMARY.md` when done
</output>
