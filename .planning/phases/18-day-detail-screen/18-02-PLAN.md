---
phase: 18-day-detail-screen
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - lib/core/router/app_router.dart
  - lib/presentation/screens/history_screen.dart
autonomous: false
requirements: [CHART-07]

must_haves:
  truths:
    - "Tapping a day with data on the calendar navigates to DayDetailScreen via push"
    - "Tapping a day with no data on the calendar does nothing"
    - "DayDetailScreen renders without bottom NavigationBar (top-level route)"
    - "Back button on DayDetailScreen returns to HistoryScreen with calendar state preserved"
    - "Dead code (_selectedDay, _buildDaySummary, AnimatedSwitcher, selectedDayPredicate) is removed from HistoryScreen"
  artifacts:
    - path: "lib/core/router/app_router.dart"
      provides: "GoRoute for /day/:dateKey top-level route"
      contains: "/day/:dateKey"
    - path: "lib/presentation/screens/history_screen.dart"
      provides: "Calendar tap navigates to DayDetailScreen, no more inline day summary"
  key_links:
    - from: "lib/presentation/screens/history_screen.dart"
      to: "lib/core/router/app_router.dart"
      via: "context.push('/day/$dateKey')"
      pattern: "context\\.push.*day"
    - from: "lib/core/router/app_router.dart"
      to: "lib/presentation/screens/day_detail_screen.dart"
      via: "GoRoute builder instantiates DayDetailScreen"
      pattern: "DayDetailScreen"
---

<objective>
Wire DayDetailScreen into GoRouter as a top-level route and modify HistoryScreen to navigate on calendar day tap, removing the old inline day summary. Implements CHART-07 (navigation from calendar to day detail via push).

Purpose: The day detail screen created in Plan 01 needs to be reachable from the calendar. The old inline day summary must be removed since it is replaced by the dedicated screen.

Output: Updated app_router.dart with /day/:dateKey route, updated history_screen.dart with push navigation and dead code removal.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-day-detail-screen/18-CONTEXT.md
@.planning/phases/18-day-detail-screen/18-PATTERNS.md
@.planning/phases/18-day-detail-screen/18-01-SUMMARY.md
</context>

## Artifacts this phase produces

### Modified files
- `lib/core/router/app_router.dart` -- new GoRoute for `/day/:dateKey`, redirect guard exemption, DayDetailScreen import
- `lib/presentation/screens/history_screen.dart` -- converted from ConsumerStatefulWidget to ConsumerWidget, removed _selectedDay field, removed _buildDaySummary method, removed AnimatedSwitcher block, removed selectedDayPredicate, added context.push navigation, added go_router import

### New symbols in modified files
- GoRoute at path `/day/:dateKey` in app_router.dart

### Removed symbols
- `_selectedDay` field (history_screen.dart)
- `selectedDayPredicate` parameter (history_screen.dart)
- `_buildDaySummary()` method (history_screen.dart)
- `AnimatedSwitcher` block wrapping _buildDaySummary (history_screen.dart)
- `_HistoryScreenState` class (history_screen.dart, replaced by ConsumerWidget)

<tasks>

<task type="auto">
  <name>Task 1: Add GoRouter route and rewire HistoryScreen navigation</name>
  <files>lib/core/router/app_router.dart, lib/presentation/screens/history_screen.dart</files>
  <read_first>
    - lib/core/router/app_router.dart (full file -- current route structure, redirect guard at lines 26-28, /calculator route pattern at lines 49-55, StatefulShellRoute starting at line 57)
    - lib/presentation/screens/history_screen.dart (full file -- _selectedDay at line 43, selectedDayPredicate at line 168, onDaySelected at lines 169-178, AnimatedSwitcher at lines 268-278, _buildDaySummary at lines 355-385, setState usage only for _selectedDay)
    - lib/presentation/screens/day_detail_screen.dart (created by Plan 01 -- verify DayDetailScreen constructor signature: required String dateKey)
  </read_first>
  <action>
Two files to modify. Per D-01, D-02, D-04, D-05 from CONTEXT.

FILE 1: lib/core/router/app_router.dart

1. Add import at top: import '../../presentation/screens/day_detail_screen.dart';

2. Add redirect guard exemption after the existing lines for /permission and /calculator (after the line checking /calculator, around line 28). Add: if (state.matchedLocation.startsWith('/day/')) return null;

3. Add new top-level GoRoute after the /calculator GoRoute (after line 55, before StatefulShellRoute). Pattern follows /calculator exactly:
   - path: '/day/:dateKey'
   - builder extracts dateKey via state.pathParameters['dateKey']! and passes to DayDetailScreen(dateKey: dateKey)
   - Add a comment matching existing style: "// /day/:dateKey is a TOP-LEVEL route (outside StatefulShellRoute) so it renders without the bottom NavigationBar."

FILE 2: lib/presentation/screens/history_screen.dart

1. Add import: import 'package:go_router/go_router.dart'; (needed for context.push)

2. Convert HistoryScreen from ConsumerStatefulWidget to ConsumerWidget. The class _HistoryScreenState has NO mutable state after _selectedDay removal:
   - Change class declaration to: class HistoryScreen extends ConsumerWidget
   - Change constructor to: const HistoryScreen({super.key});
   - Change build signature to: Widget build(BuildContext context, WidgetRef ref)
   - Remove createState method
   - Remove _HistoryScreenState class wrapper (merge build into HistoryScreen)
   - Move _buildDayCell as a private static method or top-level function (it does not use 'this' or any instance state -- it only takes context and day parameters). Keep it as a file-private top-level function to match _toDateKey and _findActiveTarget style.

3. Delete dead code per D-01:
   - Remove DateTime? _selectedDay field (line 43)
   - Remove selectedDayPredicate parameter from TableCalendar (line 168)
   - Remove the entire AnimatedSwitcher block (lines 268-278) and the SizedBox(height: 16) immediately before it (line 265) -- the spacing comment "md spacing between chart and day summary" and SizedBox go away
   - Remove the entire _buildDaySummary method (lines 355-385)

4. Replace onDaySelected callback (lines 169-178). New logic per D-01, D-02, D-05:
   - Keep the future-day guard: if (selectedDay.isAfter(DateTime.now())) return;
   - Keep the focused month update: ref.read(focusedMonthProvider.notifier).set(focusedDay);
   - Add conditional navigation: compute dateKey via _toDateKey(selectedDay). If monthTotals[dateKey] != null && monthTotals[dateKey]! > 0, call context.push('/day/$dateKey'). Otherwise do nothing (D-02: days without data do not navigate).

5. Bottom padding: the SizedBox(height: 24) at the end of the Column children list should remain (it provides padding after MonthlyBarChart, which is now the last visible widget).
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter analyze lib/core/router/app_router.dart lib/presentation/screens/history_screen.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app_router.dart contains GoRoute with path '/day/:dateKey' between /calculator route and StatefulShellRoute
    - app_router.dart contains redirect guard line: state.matchedLocation.startsWith('/day/')
    - app_router.dart imports day_detail_screen.dart
    - history_screen.dart is a ConsumerWidget (not ConsumerStatefulWidget)
    - history_screen.dart does NOT contain _selectedDay, selectedDayPredicate, AnimatedSwitcher, or _buildDaySummary
    - history_screen.dart onDaySelected calls context.push('/day/$dateKey') when monthTotals[dateKey] > 0
    - history_screen.dart imports go_router
    - flutter analyze reports no errors on either file
    - grep -c "_selectedDay" history_screen.dart returns 0
    - grep -c "_buildDaySummary" history_screen.dart returns 0
    - grep -c "AnimatedSwitcher" history_screen.dart returns 0
  </acceptance_criteria>
  <done>GoRouter has /day/:dateKey top-level route, HistoryScreen navigates to DayDetailScreen on day tap (days with data only), all dead code removed, HistoryScreen converted to ConsumerWidget, flutter analyze passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of day detail navigation and chart</name>
  <what-built>
    DayDetailScreen with per-entry bar chart, total/target display, empty state, and navigation from calendar tap. GoRouter route wired. HistoryScreen simplified (no more inline day summary).
  </what-built>
  <how-to-verify>
    1. Run the app: flutter run
    2. Go to History tab
    3. Tap a day that has water intake data (green or red circle on calendar)
    4. Verify: a new screen opens WITHOUT the bottom NavigationBar
    5. Verify: AppBar title shows the locale-formatted date (e.g. "June 16, 2026" in English, "16 giugno 2026" in Italian)
    6. Verify: a bar chart is visible with one bar per intake entry, x-axis shows HH:mm times, y-axis shows ml values
    7. Verify: above the chart (inside the same Card), the total and target text is shown (e.g. "1800 ml / 2000 ml target")
    8. Tap a bar on the chart -- verify a tooltip shows the time and ml value
    9. Press back button -- verify return to History with calendar state preserved
    10. Tap a day with NO data (no color circle) -- verify nothing happens (no navigation)
    11. Navigate to a month with no data -- verify the monthly chart shows its empty state (unchanged from Phase 17)
    12. Change device language to Italian (or any of it/fr/es) -- verify the total text and empty state text appear in the correct language
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path parameter -> screen | dateKey from GoRouter path parsed and used for provider queries |
| calendar tap -> navigation | monthTotals map consulted before navigation (only days with data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | dateKey route param | accept | Local-only app, no backend; malformed dateKey would show empty state (no entries for invalid date); no security impact |
| T-18-04 | Denial of Service | redirect guard bypass | accept | Adding /day/ to redirect exemption is correct -- day detail is not an onboarding gate; route is only reachable after onboarding completes |
</threat_model>

<verification>
- flutter analyze passes on both modified files
- No references to _selectedDay, _buildDaySummary, or AnimatedSwitcher remain in history_screen.dart
- Route /day/:dateKey exists in app_router.dart as top-level route
- Calendar day tap navigates only when data exists (monthTotals check)
- DayDetailScreen renders without bottom NavigationBar
</verification>

<success_criteria>
- Tapping a day with data on calendar navigates to DayDetailScreen (CHART-07)
- DayDetailScreen shows without NavigationBar (top-level route per D-04)
- Back navigation preserves calendar state
- Days without data do not navigate (D-02)
- Old inline day summary completely removed
</success_criteria>

<output>
Create `.planning/phases/18-day-detail-screen/18-02-SUMMARY.md` when done
</output>
