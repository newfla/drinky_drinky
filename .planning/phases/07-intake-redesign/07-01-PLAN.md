---
phase: 07-intake-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/screens/home_screen.dart
  - lib/presentation/screens/settings_screen.dart
  - lib/data/database/app_database.dart
autonomous: true
requirements: [INTAKE-01, INTAKE-02, INTAKE-03, INTAKE-04]

must_haves:
  truths:
    - "Home screen no longer shows inline quick-add buttons"
    - "A FloatingActionButton is visible on the home screen"
    - "Tapping the FAB opens a modal bottom sheet with a drag handle"
    - "The bottom sheet shows exactly 3 preset buttons matching the user's first 3 presets by sortOrder"
    - "Tapping a preset button closes the sheet, then shows a SnackBar with undo"
    - "The bottom sheet has a custom ml text field with numeric keyboard"
    - "Typing a valid amount (1-9999) and tapping Add closes the sheet, then shows a SnackBar with undo"
    - "The Add button is disabled when the text field is empty, zero, or outside 1-9999"
    - "Settings screen shows exactly 3 preset editing slots"
    - "New installs seed 3 presets (150/250/500 ml) instead of 4"
  artifacts:
    - path: "lib/presentation/screens/home_screen.dart"
      provides: "FAB, _IntakeBottomSheet widget, updated _buildContent without Row, updated empty/error state copy"
      contains: "_IntakeBottomSheet"
    - path: "lib/presentation/screens/settings_screen.dart"
      provides: "Preset card limited to 3 slots"
      contains: ".take(3)"
    - path: "lib/data/database/app_database.dart"
      provides: "3-preset seed data (150/250/500 ml)"
      contains: "amountMl: 150"
  key_links:
    - from: "home_screen.dart (FAB onPressed)"
      to: "showModalBottomSheet"
      via: "FAB.onPressed calls showModalBottomSheet with _IntakeBottomSheet"
      pattern: "showModalBottomSheet"
    - from: "_IntakeBottomSheet.onAdd callback"
      to: "home_screen.dart _onQuickAdd"
      via: "Sheet calls Navigator.pop then onAdd(amountMl) which invokes _onQuickAdd"
      pattern: "_onQuickAdd"
    - from: "settings_screen.dart _presetsCard"
      to: "drinkPresetsProvider"
      via: "ref.watch(drinkPresetsProvider) with .take(3) at presentation layer"
      pattern: "take.*3"
---

<objective>
Replace the home screen's inline quick-add buttons with a FAB that opens a modal bottom sheet containing 3 configurable presets and a custom ml input. Reduce preset count from 4 to 3 across the UI and seed data.

This plan implements all four INTAKE requirements (INTAKE-01 through INTAKE-04) and honors all locked decisions D-01 through D-12 from CONTEXT.md.

Purpose: Cleaner home screen layout with a Material 3 FAB as the sole entry point for adding intake, plus a bottom sheet that provides both quick presets and custom input in one interaction surface.

Output: Modified home_screen.dart (FAB + sheet widget + Row removal + copy updates), settings_screen.dart (.take(3)), and app_database.dart (3-preset seed).
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-intake-redesign/07-CONTEXT.md
@.planning/phases/07-intake-redesign/07-RESEARCH.md
@.planning/phases/07-intake-redesign/07-PATTERNS.md
@.planning/phases/07-intake-redesign/07-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces

### New symbols (created by this phase)
- `_IntakeBottomSheet` -- private StatefulWidget class in home_screen.dart; receives `List<DrinkPresetEntity> presets` and `void Function(int amountMl) onAdd`
- `_IntakeBottomSheetState` -- State class for `_IntakeBottomSheet`; owns `TextEditingController _controller`

### Modified symbols
- `_HomeScreenState.build()` -- gains `floatingActionButton` property on Scaffold (per D-10: inner HomeScreen Scaffold)
- `_HomeScreenState._buildContent()` -- `presets` parameter removed; quick-add Row block (lines 150-166) deleted; SizedBox(height: 24) at line 149 changed to SizedBox(height: 32)
- `_HomeScreenState._buildEmptyState()` -- body text changed to `'Tap the + button to log your first drink today.'`
- `_SettingsScreenState._presetsCard()` -- `presets.map(...)` changed to `presets.take(3).map(...)`
- `AppDatabase.migration.onCreate` -- seed block changed from 4 rows (200/300/400/500) to 3 rows (150/250/500)

### No new files created
All changes are within existing files.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Replace quick-add Row with FAB and modal bottom sheet in home_screen.dart</name>
  <files>lib/presentation/screens/home_screen.dart</files>

  <read_first>
    lib/presentation/screens/home_screen.dart
    lib/presentation/widgets/preset_edit_dialog.dart
    .planning/phases/07-intake-redesign/07-UI-SPEC.md
    .planning/phases/07-intake-redesign/07-CONTEXT.md
  </read_first>

  <action>
  Implement four changes in home_screen.dart per D-01 through D-12:

  **1. Create `_IntakeBottomSheet` StatefulWidget** (append at end of file, after the `HomeScreen` class):
  - Constructor takes `required List<DrinkPresetEntity> presets` and `required void Function(int amountMl) onAdd`. No `ref` parameter -- the sheet is stateless with respect to DB writes (per D-09).
  - State class owns a `TextEditingController _controller`, created in `initState`, disposed in `dispose` (follows the exact lifecycle pattern from `_PresetEditDialogState` in preset_edit_dialog.dart lines 28-44).
  - `build()` method layout per UI-SPEC "Modal Bottom Sheet Layout":
    - `Padding(padding: EdgeInsets.symmetric(horizontal: 16))` wrapping a `Column(mainAxisSize: MainAxisSize.min)`
    - `SizedBox(height: 16)` after drag handle
    - Preset buttons: `Row` of `widget.presets.map(...)` producing `Expanded` children each containing `Padding(padding: EdgeInsets.symmetric(horizontal: 4))` wrapping `FilledButton`. Label format: `'+\${preset.amountMl} ml'`. `onPressed`: call `Navigator.pop(context)` first, then `widget.onAdd(preset.amountMl)` (per D-01, D-03).
    - `SizedBox(height: 24)` between preset row and custom input
    - `TextField` with `controller: _controller`, `keyboardType: TextInputType.number`, `decoration: InputDecoration(hintText: 'Custom amount', suffixText: 'ml')`, `onChanged: (_) => setState(() {})` to trigger rebuild for validation
    - Validation: `final parsed = int.tryParse(_controller.text); final isValid = parsed != null && parsed >= 1 && parsed <= 9999;` (per D-11)
    - `SizedBox(height: 16)` between TextField and submit button
    - Submit: `SizedBox(width: double.infinity, child: FilledButton(onPressed: isValid ? () { Navigator.pop(context); widget.onAdd(parsed); } : null, child: Text('Add')))` (per D-02, D-03, D-12)
    - `SizedBox(height: 16)` bottom padding

  **2. Add FAB to Scaffold in `build()` method** (per D-10, INTAKE-01):
  - Add `floatingActionButton` property to the existing `Scaffold(appBar: ...)` at line 90.
  - `FloatingActionButton(tooltip: 'Add water', onPressed: () { ... }, child: Icon(Icons.add))`
  - In `onPressed`: get `final presets = presetsAsync.value ?? <DrinkPresetEntity>[];` then call `showModalBottomSheet(context: context, showDragHandle: true, useSafeArea: true, builder: (_) => _IntakeBottomSheet(presets: presets.take(3).toList(), onAdd: _onQuickAdd))`.

  **3. Remove quick-add Row from `_buildContent()`**:
  - Delete the entire Padding+Row block at lines 150-166 (the `// Quick-Add Row (HOME-02)` section).
  - Change the `SizedBox(height: 24)` at line 149 to `SizedBox(height: 32)` (per UI-SPEC: ring to timeline header becomes a single 32px gap).
  - Remove the `presets` parameter from `_buildContent()` signature (no longer used in the method body). Update the call site in `build()` accordingly.

  **4. Update copy in `_buildEmptyState()` and error state**:
  - Empty state body text (line 229): change from `'Tap a button above to log your first drink today.'` to `'Tap the + button to log your first drink today.'`
  - Error state text (line 94-96): change from `'Something went wrong loading your data.'` to `'Something went wrong loading your data. Please restart the app.'`
  </action>

  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter analyze lib/presentation/screens/home_screen.dart 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - grep -c '_IntakeBottomSheet' lib/presentation/screens/home_screen.dart returns >= 3 (class def, state class, usage in showModalBottomSheet)
  - grep -c 'floatingActionButton' lib/presentation/screens/home_screen.dart returns >= 1
  - grep -c 'showModalBottomSheet' lib/presentation/screens/home_screen.dart returns >= 1
  - grep -c 'Navigator.pop' lib/presentation/screens/home_screen.dart returns >= 2 (one for preset tap, one for custom submit)
  - grep -c 'take(3)' lib/presentation/screens/home_screen.dart returns >= 1 (in FAB onPressed)
  - grep 'Tap the + button' lib/presentation/screens/home_screen.dart returns a match
  - grep 'Please restart the app' lib/presentation/screens/home_screen.dart returns a match
  - The old quick-add Row pattern (Padding + Row + presets.map inline in _buildContent) is absent: grep -c "Quick-Add Row" lib/presentation/screens/home_screen.dart returns 0
  - _buildContent signature no longer includes a presets parameter: grep '_buildContent' lib/presentation/screens/home_screen.dart shows no DrinkPresetEntity in signature
  - flutter analyze lib/presentation/screens/home_screen.dart reports no issues
  </acceptance_criteria>

  <done>
  home_screen.dart has a FAB on the inner Scaffold that opens a modal bottom sheet with 3 preset buttons and a custom ml input. The old quick-add Row is removed. Empty state and error state copy are updated. The file compiles with no analysis issues.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduce presets to 3 in settings screen and seed data</name>
  <files>lib/presentation/screens/settings_screen.dart, lib/data/database/app_database.dart</files>

  <read_first>
    lib/presentation/screens/settings_screen.dart
    lib/data/database/app_database.dart
    .planning/phases/07-intake-redesign/07-CONTEXT.md
  </read_first>

  <action>
  Two small changes implementing INTAKE-04 and D-04/D-05/D-06:

  **1. settings_screen.dart `_presetsCard()`** (per D-06, INTAKE-04):
  - At line 129, change `presets.map((preset) {` to `presets.take(3).map((preset) {`
  - This is a single insertion of `.take(3)` before `.map()`. No other changes to the method.

  **2. app_database.dart seed block** (per D-04):
  - Replace the comment on line 44 from `// Seed default drink presets (200/300/400/500ml).` to `// Seed default drink presets (150/250/500ml).`
  - Replace the 4 `DrinkPresetsCompanion.insert(...)` calls with 3:
    - `DrinkPresetsCompanion.insert(amountMl: 150, sortOrder: 0)`
    - `DrinkPresetsCompanion.insert(amountMl: 250, sortOrder: 1)`
    - `DrinkPresetsCompanion.insert(amountMl: 500, sortOrder: 2)`
  - No migration needed (per D-05). Existing users keep their 4 DB rows; the 4th is hidden at the UI layer.
  </action>

  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter analyze lib/presentation/screens/settings_screen.dart lib/data/database/app_database.dart 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - grep -c 'take(3)' lib/presentation/screens/settings_screen.dart returns >= 1
  - grep -c 'amountMl: 150' lib/data/database/app_database.dart returns 1
  - grep -c 'amountMl: 250' lib/data/database/app_database.dart returns 1
  - grep -c 'amountMl: 500' lib/data/database/app_database.dart returns 1
  - grep -c 'amountMl: 200' lib/data/database/app_database.dart returns 0 (old seed removed)
  - grep -c 'amountMl: 300' lib/data/database/app_database.dart returns 0 (old seed removed)
  - grep -c 'amountMl: 400' lib/data/database/app_database.dart returns 0 (old seed removed)
  - grep -c 'sortOrder: 3' lib/data/database/app_database.dart returns 0 (4th preset removed)
  - flutter analyze on both files reports no issues
  </acceptance_criteria>

  <done>
  Settings screen shows exactly 3 preset editing slots via .take(3). New installs seed 3 presets (150/250/500 ml) instead of 4. No migration needed for existing users. Both files compile with no analysis issues.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> TextField | Untrusted numeric text input from soft keyboard enters the custom ml field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | _IntakeBottomSheet TextField (custom ml input) | mitigate | int.tryParse returns null for non-numeric; range guard (1-9999) disables submit button when invalid; TextInputType.number restricts keyboard to digits on most platforms (per D-11) |
| T-07-02 | Tampering | _IntakeBottomSheet preset.amountMl | accept | Preset values come from local SQLite via Drift typed query; user controls their own data in a single-user offline app; no trust boundary crossed |
| T-07-03 | Denial of Service | Rapid FAB taps opening multiple sheets | accept | showModalBottomSheet is modal -- only one instance can be open at a time; Flutter framework prevents stacking |
</threat_model>

<verification>
After both tasks complete:

1. `flutter analyze` passes with no issues across the entire project
2. `flutter build apk --debug` succeeds (build verification)
3. grep confirms all acceptance criteria from both tasks
4. The existing `flutter test` suite passes (widget_test.dart)
</verification>

<success_criteria>
1. Home screen displays a FAB (Icons.add) instead of inline quick-add buttons
2. Tapping the FAB opens a modal bottom sheet with drag handle, 3 preset buttons, and a custom ml input
3. Tapping a preset closes the sheet then shows an undo SnackBar (D-01)
4. Typing a valid amount and tapping Add closes the sheet then shows an undo SnackBar (D-02)
5. The Add button is disabled for empty, zero, negative, or >9999 values (D-11)
6. Settings screen shows exactly 3 preset slots (D-05, D-06)
7. New install seed data contains 3 presets: 150/250/500 ml (D-04)
8. Empty state text references the + button; error state includes restart instruction
9. flutter analyze reports zero issues
</success_criteria>

<output>
Create `.planning/phases/07-intake-redesign/07-01-SUMMARY.md` when done
</output>
