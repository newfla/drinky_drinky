---
phase: 06-bug-fix-theme-l-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
autonomous: false
requirements:
  - THEME-01
  - THEME-02
  - THEME-03

must_haves:
  truths:
    - "App uses Material You dynamic color on Android 12+ derived from device wallpaper"
    - "App falls back to a static blue seed palette on Android <12 and all iOS devices"
    - "App adapts to system dark mode automatically via ThemeMode.system"
  artifacts:
    - path: "pubspec.yaml"
      provides: "dynamic_color and intl direct dependencies"
      contains: "dynamic_color"
    - path: "lib/main.dart"
      provides: "DynamicColorBuilder wrapping MaterialApp.router with dual theme"
      contains: "DynamicColorBuilder"
  key_links:
    - from: "lib/main.dart"
      to: "package:dynamic_color/dynamic_color.dart"
      via: "import statement"
      pattern: "import.*dynamic_color"
    - from: "lib/main.dart"
      to: "MaterialApp.router"
      via: "DynamicColorBuilder builder callback"
      pattern: "DynamicColorBuilder"
---

<objective>
Add the dynamic_color and intl dependencies, then integrate DynamicColorBuilder into main.dart to provide Material You theming with dark mode support and a static blue seed fallback.

Purpose: Establishes the theme foundation (THEME-01, THEME-02, THEME-03) that all screens inherit via Theme.of(context). Per D-01 through D-05.
Output: pubspec.yaml with new dependencies; main.dart with DynamicColorBuilder wrapping MaterialApp.router, dual light/dark themes, and ThemeMode.system.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bug-fix-theme-l-display/06-CONTEXT.md
@.planning/phases/06-bug-fix-theme-l-display/06-RESEARCH.md
@.planning/phases/06-bug-fix-theme-l-display/06-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy for dynamic_color and intl</name>
  <what-built>
  Both packages were tagged [ASSUMED] during research because slopcheck was unavailable.
  - dynamic_color 1.8.1 — published by material.io (Google), source: github.com/material-foundation/flutter-packages
  - intl 0.20.2 — published by dart.dev (Dart team), source: github.com/dart-lang/i18n, already a transitive dependency via table_calendar
  </what-built>
  <how-to-verify>
    1. Confirm dynamic_color publisher on https://pub.dev/packages/dynamic_color — should show "Verified publisher: material.io"
    2. Confirm intl publisher on https://pub.dev/packages/intl — should show "Verified publisher: dart.dev"
    3. Both are from Google-affiliated verified publishers. Type "approved" if acceptable.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add dependencies and integrate DynamicColorBuilder</name>
  <files>pubspec.yaml, lib/main.dart</files>
  <read_first>
    - pubspec.yaml (current dependency list — verify dynamic_color and intl are absent)
    - lib/main.dart (current DrinkyDrinkyApp.build() — see colorSchemeSeed on line 38)
    - .planning/phases/06-bug-fix-theme-l-display/06-PATTERNS.md (exact current build method lines 33-43)
    - .planning/phases/06-bug-fix-theme-l-display/06-RESEARCH.md (DynamicColorBuilder integration pattern, Pitfall 1 about colorScheme vs colorSchemeSeed)
  </read_first>
  <action>
  In pubspec.yaml, add two dependencies under the dependencies section:
  - `dynamic_color: ^1.8.1` — add below the `shared_preferences` line (Utilities section), or create a "# Theming" comment section
  - `intl: ^0.20.2` — promote from transitive to direct dependency (add under Utilities section)

  Run `fvm flutter pub get` to resolve dependencies.

  In lib/main.dart:
  - Add `import 'package:dynamic_color/dynamic_color.dart';` at the top imports
  - Per D-01: Wrap the `MaterialApp.router(...)` return in `DynamicColorBuilder(builder: (ColorScheme? lightDynamic, ColorScheme? darkDynamic) { ... })`
  - Per D-02: Replace `colorSchemeSeed: Colors.blue` with `colorScheme: lightDynamic ?? ColorScheme.fromSeed(seedColor: Colors.blue)`. CRITICAL: remove `colorSchemeSeed` entirely — having both `colorScheme` and `colorSchemeSeed` causes an assertion crash (Pitfall 1)
  - Keep `useMaterial3: true` in the light theme ThemeData
  - Per D-03: Add `darkTheme: ThemeData(colorScheme: darkDynamic ?? ColorScheme.fromSeed(seedColor: Colors.blue, brightness: Brightness.dark), useMaterial3: true)`
  - Per D-04: Add `themeMode: ThemeMode.system` to MaterialApp.router
  - Per D-05: Fallback seed color is `Colors.blue` in both light and dark ColorScheme.fromSeed calls
  - Keep existing `routerConfig: router` and `title: 'Drinky Drinky'` unchanged
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && fvm flutter analyze lib/main.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `dynamic_color: ^1.8.1` as a direct dependency
    - pubspec.yaml contains `intl: ^0.20.2` as a direct dependency
    - lib/main.dart contains `import 'package:dynamic_color/dynamic_color.dart';`
    - lib/main.dart contains `DynamicColorBuilder(` wrapping MaterialApp.router
    - lib/main.dart contains `colorScheme: lightDynamic ?? ColorScheme.fromSeed(seedColor: Colors.blue)`
    - lib/main.dart does NOT contain `colorSchemeSeed` (grep returns 0 matches)
    - lib/main.dart contains `darkTheme: ThemeData(` with `ColorScheme.fromSeed(seedColor: Colors.blue, brightness: Brightness.dark)`
    - lib/main.dart contains `themeMode: ThemeMode.system`
    - `fvm flutter analyze lib/main.dart` reports no issues
  </acceptance_criteria>
  <done>DynamicColorBuilder wraps MaterialApp.router with light/dark dual themes and ThemeMode.system; colorSchemeSeed is fully replaced by colorScheme; both dependencies are direct in pubspec.yaml; flutter analyze passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev registry -> app binary | New runtime dependency (dynamic_color) enters the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | pub.dev installs (dynamic_color, intl) | mitigate | Package legitimacy checkpoint (Task 1) gates installation; both from verified Google publishers |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | N/A — Flutter/pub.dev ecosystem, not npm/pip/cargo; packages from verified publishers material.io and dart.dev |
</threat_model>

<verification>
- `fvm flutter analyze lib/main.dart` — no analysis errors
- `grep -c 'colorSchemeSeed' lib/main.dart` returns 0
- `grep -c 'DynamicColorBuilder' lib/main.dart` returns at least 1
- `grep -c 'themeMode: ThemeMode.system' lib/main.dart` returns 1
- `grep -c 'darkTheme' lib/main.dart` returns at least 1
- `grep 'dynamic_color' pubspec.yaml` returns the dependency line
- `grep 'intl:' pubspec.yaml` returns the dependency line
</verification>

<success_criteria>
App compiles with dynamic_color and intl as direct dependencies. main.dart uses DynamicColorBuilder to provide dynamic/fallback light and dark themes with ThemeMode.system. No colorSchemeSeed remains. Flutter analyze passes.
</success_criteria>

## Artifacts this phase produces

Note: This plan (01) produces the following. See plan 02 for screen-level artifacts.

- **Import**: `import 'package:dynamic_color/dynamic_color.dart'` in lib/main.dart
- **Widget wrapping**: `DynamicColorBuilder` wrapping `MaterialApp.router` in `DrinkyDrinkyApp.build()`
- **ThemeData property**: `colorScheme:` replaces `colorSchemeSeed:` in light theme
- **ThemeData property**: `darkTheme:` added to `MaterialApp.router`
- **ThemeData property**: `themeMode: ThemeMode.system` added to `MaterialApp.router`
- **Dependency**: `dynamic_color: ^1.8.1` in pubspec.yaml
- **Dependency**: `intl: ^0.20.2` in pubspec.yaml (promoted from transitive)

<output>
Create `.planning/phases/06-bug-fix-theme-l-display/06-01-SUMMARY.md` when done
</output>
