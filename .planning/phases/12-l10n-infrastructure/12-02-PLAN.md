---
phase: 12-l10n-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - lib/l10n/app_en.arb
  - lib/l10n/generated/app_localizations.dart
  - lib/l10n/generated/app_localizations_en.dart
autonomous: true
requirements: [L10N-01, L10N-02, L10N-03]

must_haves:
  truths:
    - "app_en.arb contains all ~67 English strings with semantic camelCase keys"
    - "flutter gen-l10n runs without errors and generates AppLocalizations class"
    - "flutter analyze passes with zero errors"
    - "ICU plural for dayStreak uses =0 explicitly for zero case"
    - "All placeholder strings have proper @metadata with type declarations"
    - "Generated files are committed to git (matching .g.dart convention)"
  artifacts:
    - path: "lib/l10n/app_en.arb"
      provides: "Complete English ARB template with all translatable strings"
      contains: "@@locale"
    - path: "lib/l10n/generated/app_localizations.dart"
      provides: "Generated abstract AppLocalizations class with delegate and supportedLocales"
      contains: "class AppLocalizations"
    - path: "lib/l10n/generated/app_localizations_en.dart"
      provides: "Generated English implementation"
      contains: "class AppLocalizationsEn"
  key_links:
    - from: "lib/l10n/generated/app_localizations.dart"
      to: "lib/l10n/app_en.arb"
      via: "flutter gen-l10n code generation"
      pattern: "localizationsDelegates"
    - from: "lib/main.dart"
      to: "lib/l10n/generated/app_localizations.dart"
      via: "import for localizationsDelegates and supportedLocales"
      pattern: "AppLocalizations"
---

<objective>
Create the complete English ARB template (app_en.arb) with all ~67 translatable strings
and run flutter gen-l10n to produce the generated AppLocalizations class.

Per D-01, Phase 12 produces the FULL app_en.arb -- not a skeleton. This effectively
covers L10N-05's template creation in Phase 12. Phase 13 starts with the full ARB
template already defined and focuses only on widget string replacement and creating
it/fr/es translation ARB files.

All ARB keys use camelCase semantic naming (per CONTEXT.md Claude's Discretion).
The dayStreak plural uses =0 explicitly for the zero case (safe across all 4 target
languages, per RESEARCH.md ICU Plural Rules).

Purpose: Complete the gen-l10n pipeline so the app compiles with full localization
infrastructure. After this plan, flutter analyze passes and all generated l10n files
exist.

Output: lib/l10n/app_en.arb (hand-authored), lib/l10n/generated/ (gen-l10n output).
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-l10n-infrastructure/12-CONTEXT.md
@.planning/phases/12-l10n-infrastructure/12-RESEARCH.md

# Prior plan output (need to know exact file structure from Plan 01)
@.planning/phases/12-l10n-infrastructure/12-01-SUMMARY.md

# Source files for string verification
@lib/core/router/app_router.dart
@lib/presentation/screens/home_screen.dart
@lib/presentation/screens/settings_screen.dart
@lib/presentation/screens/history_screen.dart
@lib/presentation/screens/hydration_calculator_screen.dart
@lib/presentation/screens/permission_screen.dart
@lib/presentation/widgets/preset_edit_dialog.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create complete app_en.arb with all English strings</name>
  <files>lib/l10n/app_en.arb</files>
  <action>
  Create lib/l10n/app_en.arb as a valid JSON file with "@@locale": "en" as the
  first entry. Include ALL translatable strings from the 7 source files (excluding
  notification_service.dart which is Phase 14 scope).

  Use camelCase semantic keys (per CONTEXT.md Claude's Discretion). Every string
  with a placeholder MUST have a corresponding @key metadata entry with a
  "description" and "placeholders" object declaring each placeholder's type.

  The complete key inventory (from RESEARCH.md string inventory, verified against
  source files):

  FROM app_router.dart (3 keys):
  - tabHome: "Home"
  - tabHistory: "History"
  - tabSettings: "Settings"

  FROM home_screen.dart (13 keys):
  - appTitle: "Drinky Drinky"
  - goalReached: "Goal reached!"
  - currentIntake: "{current} / {target} L" (placeholders: current String, target String)
  - todaysIntake: "Today's Intake"
  - noDrinksLogged: "No drinks logged yet"
  - noDrinksLoggedHint: "Tap the + button to log your first drink today."
  - mlAdded: "+{amount} ml added" (placeholder: amount int)
  - undo: "UNDO"
  - addWaterTooltip: "Add water"
  - presetButtonLabel: "+{amount} ml" (placeholder: amount int)
  - customAmountHint: "Custom amount"
  - addButton: "Add"
  - errorLoadingDataRestart: "Something went wrong loading your data. Please restart the app."

  FROM settings_screen.dart (20 keys):
  - settingsTitle: "Settings"
  - sectionDailyGoal: "DAILY GOAL"
  - sectionQuickAddPresets: "QUICK-ADD PRESETS"
  - sectionNotifications: "NOTIFICATIONS"
  - sectionHydration: "HYDRATION"
  - recalculateHydration: "Recalculate hydration recommendation"
  - applyFromTomorrow: "Apply from tomorrow"
  - applyFromTomorrowSubtitle: "Target changes take effect tomorrow"
  - applyFromTodaySubtitle: "Target changes take effect today"
  - presetTitle: "Preset {number}" (placeholder: number int)
  - amountMl: "{amount} ml" (placeholder: amount int)
  - notificationsDisabledBanner: "Notifications are disabled. Tap to open system Settings."
  - openButton: "Open"
  - intervalMinutes: "{minutes} min" (placeholder: minutes int)
  - doNotDisturb: "Do Not Disturb"
  - toggleOn: "On"
  - toggleOff: "Off"
  - startTime: "Start time"
  - endTime: "End time"
  - errorLoadingData: "Something went wrong loading your data."

  FROM history_screen.dart (8 keys):
  - historyTitle: "History"
  - noHistoryYet: "No history yet"
  - noHistoryYetHint: "Start logging water on the Home tab to see your history here."
  - dayStreak: "{count, plural, =0{No streak} =1{1 day streak} other{{count} day streak}}" (placeholder: count int) -- uses =0 explicitly per RESEARCH.md ICU plural rules (safe across en/it/fr/es)
  - daySummaryWithEntries: "{date} -- {total} of {target} ml" (placeholders: date String, total int, target int)
  - daySummaryNoEntries: "{date} -- No entries" (placeholder: date String)
  - calendarDayGoalMet: "{month} {day}: goal met" (placeholders: month String, day int)
  - calendarDayGoalNotMet: "{month} {day}: goal not met" (placeholders: month String, day int)
  - calendarDay: "{month} {day}" (placeholders: month String, day int)

  FROM hydration_calculator_screen.dart (17 keys):
  - calculatorTitle: "Hydration calculator"
  - sexLabel: "Sex"
  - sexMale: "Male"
  - sexFemale: "Female"
  - sexOther: "Other"
  - weightLabel: "Weight"
  - weightInputLabel: "Weight (kg)"
  - weightUnit: "kg"
  - weightValidationError: "Enter a weight between 1 and 300 kg"
  - climateLabel: "Climate"
  - climateCold: "Cold"
  - climateMild: "Mild"
  - climateWarm: "Warm"
  - climateVeryWarm: "Very warm"
  - climateHumid: "Humid"
  - yourRecommendation: "Your recommendation"
  - fillAllFields: "Fill in all fields"
  - privacyDisclaimer: "Your data (sex, weight, climate) is not saved or transmitted. The calculation happens entirely on your device."
  - useAsTarget: "Use as target"
  - skipButton: "Skip"
  - targetUpdateError: "Error updating target. Try again."
  - targetUpdated: "Target updated to {amount}" (placeholder: amount String)

  FROM permission_screen.dart (6 keys):
  - permissionTitle: "Stay hydrated with reminders"
  - permissionBody: "Drinky Drinky sends you gentle reminders to drink water throughout the day."
  - enableReminders: "Enable Reminders"
  - skipForNow: "Skip for now"
  - remindersEnabled: "Reminders enabled! You can adjust them anytime in Settings."
  - remindersDeclined: "No problem -- you can enable reminders later in your device Settings."

  FROM preset_edit_dialog.dart (5 keys):
  - editPresetTitle: "Edit Preset {number}" (placeholder: number int)
  - amountInputLabel: "Amount (ml)"
  - presetValidationError: "Enter a value between 50 and 2000"
  - cancelButton: "Cancel"
  - confirmButton: "Confirm"

  SHARED (1 key):
  - mlUnit: "ml"

  That gives approximately 73 keys total (slightly more than the ~67 estimate in
  RESEARCH.md because settings_screen.dart had a few more strings than initially
  counted, and calendarDay was a separate key from the two goal-met/not-met variants).

  Format each @metadata entry with a "description" field explaining where the string
  appears. For entries with placeholders, include a "placeholders" object with each
  placeholder's "type" (String or int).

  The file MUST be valid JSON. Use proper escaping for apostrophes in values
  (JSON does not need escaping for single quotes, but double quotes must be escaped).
  Use -- (double dash) instead of em-dash in ARB values for cross-platform safety.

  Do NOT include the notification_service.dart strings (notificationTitle,
  notificationBody) -- those are Phase 14 scope per CONTEXT.md deferred items.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && python3 -c "import json; f=open('lib/l10n/app_en.arb'); d=json.load(f); keys=[k for k in d if not k.startswith('@')]; print(f'Valid JSON with {len(keys)} keys'); assert len(keys) >= 67, f'Expected ~67+ keys, got {len(keys)}'" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
  lib/l10n/app_en.arb is valid JSON with @@locale: en.
  Contains 67+ translatable string keys with camelCase semantic names.
  All placeholder strings have @key metadata with type-declared placeholders.
  dayStreak uses ICU plural with =0 explicit case.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run flutter gen-l10n and verify compilation</name>
  <files>lib/l10n/generated/app_localizations.dart, lib/l10n/generated/app_localizations_en.dart</files>
  <action>
  1. Run flutter pub get to resolve the flutter_localizations SDK dependency
     added in Plan 01.

  2. Run flutter gen-l10n to generate the AppLocalizations class from app_en.arb.
     This is a standalone command -- NOT run through build_runner. The output goes
     to lib/l10n/generated/ per the output-dir setting in l10n.yaml.

     Expected generated files:
     - lib/l10n/generated/app_localizations.dart (abstract class, delegate,
       localizationsDelegates getter, supportedLocales list, lookupAppLocalizations)
     - lib/l10n/generated/app_localizations_en.dart (English implementation with
       all getter methods matching app_en.arb keys)

  3. Run flutter analyze to verify the entire project compiles without errors.
     The main.dart and history_screen.dart changes from Plan 01 reference
     AppLocalizations which now exists. Everything should resolve.

  4. Verify the generated files contain expected content:
     - app_localizations.dart contains class AppLocalizations
     - app_localizations.dart contains localizationsDelegates
     - app_localizations.dart contains supportedLocales
     - app_localizations_en.dart contains class AppLocalizationsEn

  If flutter gen-l10n reports errors, fix app_en.arb (likely ICU syntax issue)
  and re-run. Common issues: unbalanced braces in plural forms, missing comma
  between JSON entries, type mismatch in placeholders.

  If flutter analyze reports errors in main.dart or history_screen.dart, the
  import path may need adjustment. The correct import is:
  import 'package:drinky_drinky/l10n/generated/app_localizations.dart';

  Generated files should be committed to git (matching .g.dart convention per
  RESEARCH.md open question 3).
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && flutter gen-l10n 2>&1 && test -f lib/l10n/generated/app_localizations.dart && test -f lib/l10n/generated/app_localizations_en.dart && grep -q "class AppLocalizations" lib/l10n/generated/app_localizations.dart && flutter analyze --no-fatal-infos 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>
  flutter gen-l10n runs without errors.
  lib/l10n/generated/app_localizations.dart exists with AppLocalizations class.
  lib/l10n/generated/app_localizations_en.dart exists with AppLocalizationsEn class.
  flutter analyze passes with zero errors.
  All generated files are ready to commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tooling -> runtime | ARB files are compile-time assets processed by gen-l10n; output is type-safe Dart code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | app_en.arb | accept | Developer-authored compile-time asset. gen-l10n validates ICU syntax. Not user-supplied. |
| T-12-SC | Tampering | Dependencies | accept | No new third-party packages in this plan. flutter_localizations is SDK-bundled. |
</threat_model>

<verification>
1. flutter gen-l10n exits with code 0 (no errors)
2. flutter analyze reports 0 errors (infos/warnings acceptable)
3. lib/l10n/generated/app_localizations.dart exists and contains AppLocalizations class
4. lib/l10n/generated/app_localizations_en.dart exists and contains AppLocalizationsEn class
5. app_en.arb is valid JSON with 67+ non-@ keys
6. dayStreak key contains ICU plural with =0 case
</verification>

<success_criteria>
- app_en.arb contains complete English string inventory (~73 keys)
- flutter gen-l10n produces AppLocalizations class without errors
- flutter analyze passes with zero errors
- Generated l10n files exist in lib/l10n/generated/
- Project compiles with full localization pipeline wired
</success_criteria>

<output>
Create `.planning/phases/12-l10n-infrastructure/12-02-SUMMARY.md` when done
</output>
