---
phase: 09-data-foundation-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/database/tables/target_history_table.dart
  - lib/data/database/daos/target_history_dao.dart
  - lib/data/database/app_database.dart
autonomous: true
requirements:
  - TARGET-01

must_haves:
  truths:
    - "La tabella target_history esiste nello schema Drift con colonne id, effectiveDate (UNIQUE), targetMl"
    - "Al primo avvio, target_history contiene una riga seed con effectiveDate = oggi e targetMl = 2000"
    - "getTargetForDate restituisce il targetMl della riga piu recente con effectiveDate <= dateKey"
    - "insertOrReplace esegue upsert sulla colonna effectiveDate senza creare duplicati"
    - "watchAll restituisce uno stream reattivo di tutte le righe ordinate per effectiveDate ASC"
  artifacts:
    - path: "lib/data/database/tables/target_history_table.dart"
      provides: "Definizione tabella TargetHistory con UNIQUE su effectiveDate"
    - path: "lib/data/database/daos/target_history_dao.dart"
      provides: "DAO con getTargetForDate, watchAll, insertOrReplace"
      exports: ["TargetHistoryDao"]
    - path: "lib/data/database/app_database.dart"
      provides: "Registrazione tabella + DAO + seed row in onCreate"
      contains: "TargetHistory"
  key_links:
    - from: "lib/data/database/daos/target_history_dao.dart"
      to: "lib/data/database/tables/target_history_table.dart"
      via: "@DriftAccessor(tables: [TargetHistory])"
      pattern: "DriftAccessor.*TargetHistory"
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/tables/target_history_table.dart"
      via: "@DriftDatabase tables list"
      pattern: "tables:.*TargetHistory"
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/daos/target_history_dao.dart"
      via: "@DriftDatabase daos list"
      pattern: "daos:.*TargetHistoryDao"
---

<objective>
Creare la tabella Drift `target_history` con DAO completo e seed row per la Phase 9 (D-04, D-05, D-06, D-07).

Purpose: La tabella target_history e il fondamento per tracciare lo storico dei target giornalieri. Phase 10 e 11 dipendono da questa tabella per mostrare il target corretto per ogni giorno e per il calcolatore di idratazione.

Output: Tabella target_history registrata nello schema, DAO con 3 metodi (getTargetForDate, watchAll, insertOrReplace), seed row in onCreate. Code-gen eseguito con build_runner.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-foundation-bug-fixes/09-CONTEXT.md
@.planning/phases/09-data-foundation-bug-fixes/09-RESEARCH.md
@.planning/phases/09-data-foundation-bug-fixes/09-PATTERNS.md
</context>

## Artifacts this phase produces

**New files:**
- `lib/data/database/tables/target_history_table.dart` -- class `TargetHistory extends Table` con colonne `id`, `effectiveDate` (text, unique), `targetMl` (integer)
- `lib/data/database/daos/target_history_dao.dart` -- class `TargetHistoryDao extends DatabaseAccessor<AppDatabase>` con mixin `_$TargetHistoryDaoMixin`
- `lib/data/database/daos/target_history_dao.g.dart` -- generato da build_runner
- `test/data/database/daos/target_history_dao_test.dart` -- (Plan 02)
- `test/data/repositories/water_repository_test.dart` -- (Plan 02)

**New symbols:**
- `TargetHistory` (Table class in target_history_table.dart)
- `TargetHistoryDao` (DAO class in target_history_dao.dart)
- `TargetHistoryDao.getTargetForDate(String dateKey)` -- `Future<int?>`
- `TargetHistoryDao.watchAll()` -- `Stream<List<TargetHistoryData>>`
- `TargetHistoryDao.insertOrReplace(String effectiveDate, int targetMl)` -- `Future<void>`
- Generated: `$TargetHistoryTable`, `TargetHistoryData` (o nome generato equivalente), `TargetHistoryCompanion`, `_$TargetHistoryDaoMixin`

**Modified files:**
- `lib/data/database/app_database.dart` -- aggiunta TargetHistory a tables, TargetHistoryDao a daos, seed row in onCreate
- `lib/data/database/app_database.g.dart` -- rigenerato
- `test/data/database/daos/water_entry_dao_test.dart` -- (Plan 02, nuovo test BUG-01)

<tasks>

<task type="auto">
  <name>Task 1: Definire la tabella TargetHistory e il DAO</name>
  <files>
    lib/data/database/tables/target_history_table.dart,
    lib/data/database/daos/target_history_dao.dart
  </files>
  <read_first>
    lib/data/database/tables/water_entries_table.dart,
    lib/data/database/daos/water_entry_dao.dart,
    lib/data/database/app_database.dart
  </read_first>
  <action>
    Creare `lib/data/database/tables/target_history_table.dart` (per D-04, D-06):
    - Classe `TargetHistory extends Table` con 3 colonne:
      - `id`: `integer().autoIncrement()()`
      - `effectiveDate`: `text().unique()()` -- formato YYYY-MM-DD, vincolo UNIQUE (per D-06, usando `text().unique()` come raccomandato dalla ricerca, non `uniqueKeys` override)
      - `targetMl`: `integer()()`
    - Nessun `@TableIndex` necessario (il vincolo UNIQUE crea implicitamente un indice)
    - Import: solo `package:drift/drift.dart`

    Creare `lib/data/database/daos/target_history_dao.dart` (per D-07):
    - Seguire esattamente il pattern di `water_entry_dao.dart`: import drift + app_database + table file, `part` directive per `.g.dart`, `@DriftAccessor(tables: [TargetHistory])`, extends `DatabaseAccessor<AppDatabase> with _$TargetHistoryDaoMixin`, constructor `TargetHistoryDao(super.attachedDatabase)`
    - Metodo `getTargetForDate(String dateKey)` -> `Future<int?>`: `select(targetHistory)` con `where` usando `isSmallerOrEqualValue(dateKey)`, `orderBy DESC` su `effectiveDate`, `limit(1)`, `getSingleOrNull()`, return `row?.targetMl`
    - Metodo `watchAll()` -> `Stream<List<...>>`: `select(targetHistory)` con `orderBy ASC` su `effectiveDate`, `.watch()`
    - Metodo `insertOrReplace(String effectiveDate, int targetMl)` -> `Future<void>`: CRITICO -- NON usare `insertOnConflictUpdate()` (funziona solo su PK). Usare `into(targetHistory).insert(TargetHistoryCompanion.insert(effectiveDate: effectiveDate, targetMl: targetMl), onConflict: DoUpdate((old) => TargetHistoryCompanion.custom(targetMl: Constant(targetMl)), target: [targetHistory.effectiveDate]))`
    - Il tipo di ritorno dello stream di watchAll usera il data class generato da Drift. Dopo build_runner verificare il nome esatto in app_database.g.dart (potrebbe essere `TargetHistoryData` per tabella singolare).
  </action>
  <verify>
    <automated>ls lib/data/database/tables/target_history_table.dart lib/data/database/daos/target_history_dao.dart && grep -c "TargetHistory" lib/data/database/tables/target_history_table.dart && grep -c "insertOrReplace" lib/data/database/daos/target_history_dao.dart</automated>
  </verify>
  <acceptance_criteria>
    - Il file `target_history_table.dart` contiene `class TargetHistory extends Table` con esattamente 3 colonne getter (id, effectiveDate, targetMl)
    - `effectiveDate` usa `text().unique()()` (NON `text()()` senza unique)
    - Il file `target_history_dao.dart` contiene `class TargetHistoryDao extends DatabaseAccessor<AppDatabase> with _$TargetHistoryDaoMixin`
    - Il DAO ha esattamente 3 metodi: `getTargetForDate`, `watchAll`, `insertOrReplace`
    - `insertOrReplace` usa `DoUpdate` con `target: [targetHistory.effectiveDate]` (NON `insertOnConflictUpdate`)
    - `getTargetForDate` usa `isSmallerOrEqualValue` + `OrderingTerm.desc` + `limit(1)` + `getSingleOrNull`
  </acceptance_criteria>
  <done>Tabella TargetHistory e DAO con 3 metodi creati, pronti per la registrazione in app_database.dart</done>
</task>

<task type="auto">
  <name>Task 2: Registrare tabella e DAO, aggiungere seed, eseguire code-gen</name>
  <files>
    lib/data/database/app_database.dart
  </files>
  <read_first>
    lib/data/database/app_database.dart,
    lib/data/database/tables/target_history_table.dart,
    lib/data/database/daos/target_history_dao.dart,
    lib/core/providers/stream_providers.dart
  </read_first>
  <action>
    Modificare `lib/data/database/app_database.dart` (per D-04, D-05):

    1. Aggiungere due import in cima (dopo gli import esistenti):
       - `import 'tables/target_history_table.dart';`
       - `import 'daos/target_history_dao.dart';`

    2. Nella annotation `@DriftDatabase`:
       - `tables:` aggiungere `TargetHistory` alla lista: `[WaterEntries, UserSettings, DrinkPresets, TargetHistory]`
       - `daos:` aggiungere `TargetHistoryDao` alla lista: `[WaterEntryDao, UserSettingsDao, DrinkPresetDao, TargetHistoryDao]`

    3. `schemaVersion` RESTA a `1` (per D-04 -- prima vera installazione, nessuna migration)

    4. In `MigrationStrategy.onCreate`, DOPO il blocco `batch` esistente (riga 51), aggiungere la seed row per target_history (per D-05):
       - Calcolare `todayKey` con lo stesso pattern inline di `stream_providers.dart:57-60`: `final now = DateTime.now(); final todayKey = '${now.year}-${now.month.toString().padLeft(2, '0')}-${now.day.toString().padLeft(2, '0')}';`
       - NON importare `todayDateKey()` dal layer providers (causerebbe dipendenza circolare database -> providers)
       - Inserire: `await into(targetHistory).insert(TargetHistoryCompanion.insert(effectiveDate: todayKey, targetMl: 2000));`
       - Il valore `2000` corrisponde al default di `UserSettings.dailyTargetMl` (`Constant(2000)` in user_settings_table.dart)

    5. Eseguire `dart run build_runner build --delete-conflicting-outputs` per rigenerare:
       - `app_database.g.dart` (con `$TargetHistoryTable`, accessor `targetHistory`, `TargetHistoryDao` mixin)
       - `target_history_dao.g.dart` (nuovo file con `_$TargetHistoryDaoMixin`)

    6. Dopo build_runner, verificare in `app_database.g.dart` il nome esatto del data class generato per la tabella TargetHistory. Se il nome non corrisponde a quello usato nel DAO `watchAll()`, aggiornare il tipo di ritorno del DAO.

    7. Verificare compilazione: `dart analyze lib/data/database/`
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && dart analyze lib/data/database/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `@DriftDatabase` annotation contiene `TargetHistory` in `tables:` e `TargetHistoryDao` in `daos:`
    - `app_database.dart` ha import per `target_history_table.dart` e `target_history_dao.dart`
    - `schemaVersion` e ancora `1` (non incrementato)
    - `onCreate` contiene l'insert di `targetHistory` con `effectiveDate: todayKey` e `targetMl: 2000`
    - `todayKey` e calcolato inline (nessun import da providers layer)
    - `app_database.g.dart` contiene la classe `$TargetHistoryTable`
    - `target_history_dao.g.dart` esiste e contiene `_$TargetHistoryDaoMixin`
    - `dart analyze lib/data/database/` non riporta errori
    - build_runner completa senza errori
  </acceptance_criteria>
  <done>Tabella TargetHistory registrata nello schema, seed row attiva in onCreate, code-gen completata, zero errori di analisi</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAO input (effectiveDate, targetMl) | Stringa dateKey e intero ml passati al DAO da chiamanti interni |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | target_history.effectiveDate | mitigate | UNIQUE constraint su effectiveDate impedisce righe duplicate; upsert via DoUpdate con target esplicito |
| T-09-02 | Tampering | insertOrReplace dateKey format | accept | Il DAO accetta qualsiasi stringa; la validazione avverra a livello repository/provider in Phase 10 (stessa separazione usata per WaterEntryDao/WaterRepository) |
| T-09-03 | Information Disclosure | target_history data | accept | Dati locali (ml target), nessun PII, nessuna rete -- rischio irrilevante |
</threat_model>

<verification>
- `dart analyze lib/data/database/` termina senza errori
- `dart run build_runner build --delete-conflicting-outputs` completa con successo
- Il file `app_database.g.dart` contiene riferimenti a `$TargetHistoryTable` e `TargetHistoryDao`
</verification>

<success_criteria>
- La tabella `target_history` esiste nello schema Drift iniziale con 3 colonne (id autoIncrement, effectiveDate text unique, targetMl integer)
- Il DAO `TargetHistoryDao` espone 3 metodi: getTargetForDate, watchAll, insertOrReplace
- La seed row viene inserita in onCreate con effectiveDate = oggi e targetMl = 2000
- Il codice compila senza errori dopo build_runner
</success_criteria>

<output>
Create `.planning/phases/09-data-foundation-bug-fixes/09-01-SUMMARY.md` when done
</output>
