---
phase: 09-data-foundation-bug-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - test/data/database/daos/target_history_dao_test.dart
  - test/data/database/daos/water_entry_dao_test.dart
  - test/data/repositories/water_repository_test.dart
autonomous: true
requirements:
  - BUG-01
  - BUG-03
  - TARGET-01

must_haves:
  truths:
    - "deleteLastEntry con un dateKey non cancella entry di un altro giorno (BUG-01 confermato)"
    - "insertEntry rifiuta dateKey semanticamente invalidi come 2024-02-30 con ArgumentError (BUG-03 confermato)"
    - "insertEntry rifiuta dateKey malformati come abcd-ef-gh con ArgumentError (BUG-03 confermato)"
    - "getTargetForDate restituisce il target della riga con effectiveDate piu recente <= dateKey"
    - "getTargetForDate restituisce null se la tabella e vuota (difensivo)"
    - "insertOrReplace aggiorna targetMl se effectiveDate esiste gia senza creare duplicati"
    - "watchAll restituisce tutte le righe ordinate per effectiveDate ASC"
    - "La seed row di onCreate e visibile nei test DAO (effectiveDate = oggi, targetMl = 2000)"
  artifacts:
    - path: "test/data/database/daos/target_history_dao_test.dart"
      provides: "Test per getTargetForDate, watchAll, insertOrReplace, seed row"
    - path: "test/data/database/daos/water_entry_dao_test.dart"
      provides: "Test di conferma BUG-01 (deleteLastEntry cross-day)"
    - path: "test/data/repositories/water_repository_test.dart"
      provides: "Test di conferma BUG-03 (dateKey validation semantica)"
  key_links:
    - from: "test/data/database/daos/target_history_dao_test.dart"
      to: "lib/data/database/daos/target_history_dao.dart"
      via: "db.targetHistoryDao"
      pattern: "targetHistoryDao"
    - from: "test/data/repositories/water_repository_test.dart"
      to: "lib/data/repositories/water_repository.dart"
      via: "WaterRepository(db)"
      pattern: "WaterRepository"
---

<objective>
Aggiungere test di conferma per BUG-01 e BUG-03 (gia implementati nel codice) e test completi per il nuovo TargetHistoryDao (per D-01, D-02, D-03, D-07).

Purpose: I test confermano che i bug fix esistenti funzionano correttamente e che il nuovo DAO target_history opera come specificato. Questa e la garanzia che Phase 10 e 11 possono costruire su fondamenta solide.

Output: 3 file di test con copertura per: deleteLastEntry cross-day isolation, dateKey validation semantica, TargetHistoryDao getTargetForDate/watchAll/insertOrReplace/seed.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-foundation-bug-fixes/09-CONTEXT.md
@.planning/phases/09-data-foundation-bug-fixes/09-RESEARCH.md
@.planning/phases/09-data-foundation-bug-fixes/09-PATTERNS.md
@.planning/phases/09-data-foundation-bug-fixes/09-01-SUMMARY.md
</context>

## Artifacts this phase produces

**New files:**
- `test/data/database/daos/target_history_dao_test.dart` -- test per TargetHistoryDao (getTargetForDate, watchAll, insertOrReplace, seed)
- `test/data/repositories/water_repository_test.dart` -- test di conferma BUG-03 (dateKey validation)

**Modified files:**
- `test/data/database/daos/water_entry_dao_test.dart` -- aggiunto test di conferma BUG-01

**New test functions:**
- BUG-01 test: `deleteLastEntry does not delete entries from other dates`
- BUG-03 tests: `rejects semantically invalid date like 2024-02-30`, `rejects malformed dateKey like abcd-ef-gh`, `accepts valid dateKey`
- TargetHistoryDao tests: `seed row exists after onCreate`, `getTargetForDate returns seed target for today`, `getTargetForDate returns most recent target on or before dateKey`, `getTargetForDate returns null for date before any entry`, `insertOrReplace inserts new row`, `insertOrReplace updates existing row for same date`, `watchAll returns rows ordered by effectiveDate ASC`

<tasks>

<task type="auto">
  <name>Task 1: Test di conferma BUG-01 e BUG-03</name>
  <files>
    test/data/database/daos/water_entry_dao_test.dart,
    test/data/repositories/water_repository_test.dart
  </files>
  <read_first>
    test/data/database/daos/water_entry_dao_test.dart,
    lib/data/database/daos/water_entry_dao.dart,
    lib/data/repositories/water_repository.dart
  </read_first>
  <action>
    Per D-03 (test di conferma per BUG-01 e BUG-03):

    **1. BUG-01 -- Aggiungere test a `water_entry_dao_test.dart`:**
    Aggiungere un nuovo `test()` dentro il `group('WaterEntryDao', ...)` esistente (dopo l'ultimo test alla riga 181). Il test deve:
    - Inserire un'entry per ieri con dateKey `'2026-06-02'` (amountMl: 500, loggedAt: DateTime(2026, 6, 2, 20, 0))
    - Inserire un'entry per oggi con dateKey `'2026-06-03'` (amountMl: 250, loggedAt: DateTime(2026, 6, 3, 8, 0))
    - Chiamare `db.waterEntryDao.deleteLastEntry('2026-06-03')`
    - Verificare che le entry di ieri sono ancora li: `watchEntriesForDate('2026-06-02').first` ha lunghezza 1 con amountMl 500
    - Verificare che le entry di oggi sono vuote: `watchEntriesForDate('2026-06-03').first` e `isEmpty`
    - Nome test: `'deleteLastEntry does not delete entries from other dates (BUG-01)'`

    **2. BUG-03 -- Creare nuovo file `test/data/repositories/water_repository_test.dart`:**
    - Creare la directory `test/data/repositories/` se non esiste
    - Usare lo stesso setup pattern di `water_entry_dao_test.dart`: `AppDatabase(DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true))`
    - Aggiungere `late WaterRepository repo;` e inizializzare `repo = WaterRepository(db);` in setUp
    - Import aggiuntivo: `import 'package:drinky_drinky/data/repositories/water_repository.dart';`
    - Group `'WaterRepository dateKey validation (BUG-03)'` con 3 test:
      a. `'rejects semantically invalid date like 2024-02-30'`: `expect(() => repo.insertEntry(250, DateTime.now(), '2024-02-30'), throwsA(isA<ArgumentError>()))`
      b. `'rejects malformed dateKey like abcd-ef-gh'`: `expect(() => repo.insertEntry(250, DateTime.now(), 'abcd-ef-gh'), throwsA(isA<ArgumentError>()))`
      c. `'accepts valid dateKey like 2026-06-03'`: `await repo.insertEntry(250, DateTime(2026, 6, 3, 10, 0), '2026-06-03')` -- deve completare senza eccezione
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart test test/data/database/daos/water_entry_dao_test.dart test/data/repositories/water_repository_test.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `water_entry_dao_test.dart` contiene un test con nome che include "BUG-01" e verifica che deleteLastEntry('2026-06-03') non cancella entry con dateKey '2026-06-02'
    - `water_repository_test.dart` esiste in `test/data/repositories/`
    - `water_repository_test.dart` ha 3 test: uno per '2024-02-30' (throwsA ArgumentError), uno per 'abcd-ef-gh' (throwsA ArgumentError), uno per '2026-06-03' (non lancia)
    - Il setup di `water_repository_test.dart` crea sia `AppDatabase` (in-memory) sia `WaterRepository(db)`
    - Tutti i test passano con `dart test`
  </acceptance_criteria>
  <done>Test di conferma BUG-01 e BUG-03 passano -- i fix esistenti funzionano correttamente</done>
</task>

<task type="auto">
  <name>Task 2: Test completi per TargetHistoryDao</name>
  <files>
    test/data/database/daos/target_history_dao_test.dart
  </files>
  <read_first>
    lib/data/database/daos/target_history_dao.dart,
    lib/data/database/app_database.dart,
    test/data/database/daos/water_entry_dao_test.dart
  </read_first>
  <action>
    Creare `test/data/database/daos/target_history_dao_test.dart` (per D-07, verifica TARGET-01):

    Setup: stesso pattern di `water_entry_dao_test.dart` -- `AppDatabase(DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true))`, setUp/tearDown.

    Import: `package:drift/drift.dart`, `package:drift/native.dart`, `package:flutter_test/flutter_test.dart`, `package:drinky_drinky/data/database/app_database.dart`.

    Group `'TargetHistoryDao'` con i seguenti test:

    1. `'seed row exists after database creation'`: Chiamare `db.targetHistoryDao.watchAll().first`. Verificare che la lista ha esattamente 1 elemento. Verificare che `targetMl` e `2000`. Verificare che `effectiveDate` corrisponde alla data di oggi nel formato YYYY-MM-DD (calcolare inline come nel seed di onCreate).

    2. `'getTargetForDate returns seed target for today'`: Calcolare la data di oggi come stringa YYYY-MM-DD. Chiamare `db.targetHistoryDao.getTargetForDate(todayKey)`. Aspettarsi `2000`.

    3. `'getTargetForDate returns most recent target on or before dateKey'`: Inserire due righe via `db.targetHistoryDao.insertOrReplace('2026-01-01', 1500)` e `db.targetHistoryDao.insertOrReplace('2026-06-01', 2500)`. Chiamare `getTargetForDate('2026-03-15')`. Aspettarsi `1500` (la riga del 2026-01-01 e la piu recente <= 2026-03-15). Chiamare `getTargetForDate('2026-07-01')`. Aspettarsi `2500`.

    4. `'getTargetForDate returns null for date before any entry'`: Inserire una riga via `insertOrReplace('2026-06-01', 2000)`. Chiamare `getTargetForDate('2025-01-01')`. Aspettarsi `null`. (NOTA: il seed di onCreate usa la data di oggi, quindi per testare il null bisogna usare una data precedente a quella di oggi E a tutte le righe inserite. Il seed avra effectiveDate = oggi. Basta usare un dateKey molto vecchio come '2020-01-01' che sia prima del seed.)

    5. `'insertOrReplace inserts new row for new date'`: Inserire via `insertOrReplace('2030-01-01', 3000)`. Verificare con `watchAll().first` che la lista contiene la nuova riga (oltre al seed). Verificare che la riga con effectiveDate '2030-01-01' ha targetMl 3000.

    6. `'insertOrReplace updates existing row for same date (upsert)'`: Inserire via `insertOrReplace('2026-06-15', 1800)`. Poi `insertOrReplace('2026-06-15', 2200)`. Verificare con `watchAll().first` che esiste UNA SOLA riga con effectiveDate '2026-06-15' e che targetMl e `2200` (non 1800, non duplicata).

    7. `'watchAll returns rows ordered by effectiveDate ASC'`: Inserire righe per '2026-12-01', '2026-01-01', '2026-06-01' (in ordine non cronologico). Chiamare `watchAll().first`. Verificare che le effectiveDate della lista risultante sono in ordine crescente. (Ricordare che il seed di oggi sara anche nella lista.)
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart test test/data/database/daos/target_history_dao_test.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `target_history_dao_test.dart` esiste in `test/data/database/daos/`
    - Il file contiene almeno 7 test nel group 'TargetHistoryDao'
    - Il test seed verifica che watchAll restituisce 1 riga con targetMl == 2000
    - Il test getTargetForDate verifica il comportamento "most recent <= dateKey"
    - Il test getTargetForDate per data antecedente restituisce null
    - Il test upsert verifica che due insert per la stessa data producono UNA sola riga con il valore aggiornato
    - Il test watchAll verifica l'ordine ASC per effectiveDate
    - Tutti i 7 test passano con `dart test`
  </acceptance_criteria>
  <done>Test TargetHistoryDao completi -- seed, getTargetForDate, insertOrReplace (upsert), watchAll tutti verificati e passano</done>
</task>

<task type="auto">
  <name>Task 3: Esecuzione completa della test suite</name>
  <files>
    (nessun file modificato -- verifica finale)
  </files>
  <read_first>
    test/data/database/daos/target_history_dao_test.dart,
    test/data/database/daos/water_entry_dao_test.dart,
    test/data/repositories/water_repository_test.dart
  </read_first>
  <action>
    Eseguire l'intera test suite per verificare che nessun test esistente sia stato rotto dalle modifiche della Phase 9:
    - Comando: `dart test` (senza filtri -- tutti i test)
    - Verificare che TUTTI i test passano (sia i nuovi che gli 11 esistenti)
    - Se qualche test fallisce, diagnosticare e correggere prima di chiudere il plan
    - Verificare anche `dart analyze` sull'intero progetto per assicurarsi che non ci siano warning o errori introdotti
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart test 2>&1 | tail -15 && dart analyze 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dart test` riporta 0 fallimenti (tutti i test passano, inclusi gli 11 pre-esistenti + i nuovi BUG-01, BUG-03, TargetHistoryDao)
    - `dart analyze` non riporta errori (warning pre-esistenti sono accettabili)
    - Il numero totale di test e >= 21 (11 esistenti + 1 BUG-01 + 3 BUG-03 + almeno 7 TargetHistoryDao = 22 minimo atteso)
  </acceptance_criteria>
  <done>Intera test suite passa senza regressioni -- Phase 9 data foundation completata</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test input -> DAO | I test passano dateKey e targetMl direttamente al DAO; nessun boundary di sicurezza in un test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Test assertions | accept | I test sono codice locale di sviluppo, nessun rischio di tampering in contesto CI/CD |
</threat_model>

<verification>
- `dart test` completa con tutti i test che passano
- `dart analyze` non riporta errori
- I test BUG-01 e BUG-03 confermano che i fix gia implementati funzionano
- I test TargetHistoryDao coprono seed, getTargetForDate, insertOrReplace (upsert), watchAll
</verification>

<success_criteria>
- Test di conferma BUG-01 passa: deleteLastEntry non cancella entry di altri giorni
- Test di conferma BUG-03 passano: dateKey invalidi (2024-02-30, abcd-ef-gh) producono ArgumentError
- Test TargetHistoryDao passano: seed row presente, getTargetForDate corretto, upsert senza duplicati, watchAll ordinato
- Intera test suite (22+ test) passa senza regressioni
</success_criteria>

<output>
Create `.planning/phases/09-data-foundation-bug-fixes/09-02-SUMMARY.md` when done
</output>
