---
phase: 16-project-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/images/.gitkeep
autonomous: false
requirements:
  - DOC-01
must_haves:
  truths:
    - "A developer reading README.md understands what Drinky Drinky is and what it does"
    - "A developer can follow the build instructions to clone and run the app"
    - "README.md references two home screen screenshots with relative paths that will render on GitHub"
  artifacts:
    - path: "README.md"
      provides: "Project README with description, screenshots, and build instructions"
      contains: "Drinky Drinky"
    - path: "docs/images/.gitkeep"
      provides: "Screenshot directory placeholder committed to git"
  key_links:
    - from: "README.md"
      to: "docs/images/home_ios.png"
      via: "Markdown image reference"
      pattern: "!\\[.*\\]\\(docs/images/home_ios\\.png\\)"
    - from: "README.md"
      to: "docs/images/home_android.png"
      via: "Markdown image reference"
      pattern: "!\\[.*\\]\\(docs/images/home_android\\.png\\)"
---

<objective>
Replace the default Flutter placeholder README.md with a proper project README for Drinky Drinky that describes the app, embeds two home screen screenshot references (iOS and Android), lists key features, and provides standard build instructions. Create the docs/images/ directory with a .gitkeep so the screenshot path is ready for the developer to drop in files.

Implements: DOC-01, D-01, D-02, D-03, D-04, D-05, D-06, D-07.

Purpose: A developer or reviewer landing on the repository can understand the project and build it.
Output: README.md (root), docs/images/.gitkeep
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-project-readme/16-CONTEXT.md
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screenshot directory and write README.md</name>
  <files>README.md, docs/images/.gitkeep</files>
  <read_first>
    - README.md (current placeholder to be fully replaced)
    - .planning/PROJECT.md (authoritative project description and feature list)
    - .planning/phases/16-project-readme/16-CONTEXT.md (locked decisions D-01 through D-07)
    - pubspec.yaml (app name, description, dependencies for tech stack mention)
  </read_first>
  <action>
    Create the directory docs/images/ and add an empty .gitkeep file inside it (per D-02).

    Fully replace README.md with a new file structured as follows (per D-04, all content in English):

    Section 1 -- Title and description (per D-07):
    H1 heading: "Drinky Drinky" with a water drop emoji prefix.
    One-paragraph description drawn from PROJECT.md "What This Is" section. Must mention: Flutter mobile app, daily water intake tracking, daily goal, configurable quick-add buttons, monthly calendar history, configurable reminder notifications, iOS and Android, fully offline (no backend, no accounts). Include a one-line tech stack mention: "Built with Flutter, Riverpod, and Drift."

    Section 2 -- Screenshots (per D-01, D-02, D-03):
    H2 heading: "Screenshots".
    Two Markdown images side by side in a table or sequential display. Exact syntax:
    - Alt text "Home screen -- iOS", path docs/images/home_ios.png
    - Alt text "Home screen -- Android", path docs/images/home_android.png
    Use a Markdown table with two columns (iOS | Android) and one image per cell. Below the table, add an italic note: "Screenshots are added manually -- see docs/images/ for expected file names."

    Section 3 -- Features (per D-07):
    H2 heading: "Features".
    Bulleted list covering these items (derive phrasing from PROJECT.md validated requirements):
    - Animated circular progress ring showing daily hydration progress
    - Quick-add water intake via FAB-triggered bottom sheet with configurable presets
    - Undo last intake entry
    - Chronological timeline of today's individual intakes
    - Configurable daily water target in ml
    - Monthly calendar view with color-coded days (green = goal met, red = missed)
    - Consecutive-day streak counter
    - Configurable reminder notifications with DND window support
    - Built-in hydration calculator (sex/weight/climate)
    - Multilingual: English, Italian, French, Spanish
    - Material You dynamic color theming on Android 12+
    - Fully offline -- all data stored locally on device

    Section 4 -- Getting Started (per D-05):
    H2 heading: "Getting Started".
    H3 subheading: "Prerequisites".
    Bullet: Flutter SDK (>= 3.38.1) installed and on PATH.
    H3 subheading: "Build and Run".
    Numbered steps with fenced shell code blocks:
    1. git clone https://github.com/user/drinky_drinky.git followed by cd drinky_drinky (use a generic GitHub URL placeholder)
    2. flutter pub get
    3. dart run build_runner build --delete-conflicting-outputs (add a brief note: "Required -- the app uses Drift and Riverpod code generation")
    4. flutter run

    Section 5 -- Platform Requirements (per D-06):
    H3 subheading: "Platform Requirements" (under Getting Started).
    A small Markdown table with two rows:
    - Android: compileSdk 36, minSdk 26 (Android 8.0+)
    - iOS: iOS 13+

    Section 6 -- License:
    H2 heading: "License". Single line: "This project is not currently published under an open-source license."

    Do NOT include: badges, CI status, contributor guidelines, changelog, or any content not specified above.
  </action>
  <verify>
    <automated>grep -c "Drinky Drinky" README.md | grep -q "^[1-9]" && grep -c "docs/images/home_ios.png" README.md | grep -q "^[1-9]" && grep -c "docs/images/home_android.png" README.md | grep -q "^[1-9]" && grep -c "flutter pub get" README.md | grep -q "^[1-9]" && grep -c "build_runner" README.md | grep -q "^[1-9]" && grep -c "flutter run" README.md | grep -q "^[1-9]" && grep -c "compileSdk 36" README.md | grep -q "^[1-9]" && grep -c "iOS 13" README.md | grep -q "^[1-9]" && test -f docs/images/.gitkeep && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md first H1 line contains "Drinky Drinky"
    - README.md contains the string "docs/images/home_ios.png" in a Markdown image reference
    - README.md contains the string "docs/images/home_android.png" in a Markdown image reference
    - README.md contains "flutter pub get" in a code block
    - README.md contains "dart run build_runner build --delete-conflicting-outputs" in a code block
    - README.md contains "flutter run" in a code block
    - README.md contains "compileSdk 36" and "minSdk 26" and "iOS 13"
    - README.md contains at least 8 feature bullet items
    - README.md contains "Flutter" and "Riverpod" and "Drift" (tech stack mention)
    - README.md does NOT contain "A new Flutter project" (placeholder removed)
    - File docs/images/.gitkeep exists and is empty
    - README.md is written entirely in English (per D-04)
  </acceptance_criteria>
  <done>README.md fully replaced with project description, screenshot references, feature list, and build instructions. docs/images/.gitkeep exists. No trace of the default Flutter placeholder content remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer verifies README content and drops in screenshots</name>
  <files>docs/images/home_ios.png, docs/images/home_android.png</files>
  <action>
    Human checkpoint: developer reviews the README.md content for accuracy, takes two home screen screenshots (iOS simulator and Android emulator), and saves them as docs/images/home_ios.png and docs/images/home_android.png per D-01.
  </action>
  <what-built>
    README.md has been written with the full project description, screenshot image references, feature list, and build instructions. The docs/images/ directory exists with a .gitkeep placeholder.

    The README references two screenshot files that do not yet exist:
    - docs/images/home_ios.png
    - docs/images/home_android.png
  </what-built>
  <how-to-verify>
    1. Open README.md in your editor or view it on GitHub (push the branch if needed) -- verify the content reads well and is accurate
    2. Verify the build instructions are correct by reading steps 1-4 in the "Getting Started" section
    3. Verify the feature list matches what the app actually does
    4. Take a home screen screenshot on an iOS simulator and save it as docs/images/home_ios.png
    5. Take a home screen screenshot on an Android emulator and save it as docs/images/home_android.png
    6. After adding screenshots, view README.md again to confirm images render (on GitHub or with a Markdown previewer)
  </how-to-verify>
  <verify>
    <automated>test -f docs/images/home_ios.png && test -f docs/images/home_android.png && echo "PASS"</automated>
  </verify>
  <done>Developer has reviewed README.md content, confirmed accuracy, and added both screenshot files to docs/images/.</done>
  <resume-signal>Type "approved" once README content is verified and screenshots are in place, or describe any issues to fix</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

**Files created:**
- `README.md` (root) -- full project README replacing the Flutter placeholder
- `docs/images/.gitkeep` -- empty placeholder ensuring the screenshot directory is committed

**No new code symbols** -- this phase is documentation-only. No classes, functions, decorators, or exports are created.

<threat_model>
## Trust Boundaries

No trust boundaries in this phase. Documentation-only changes: Markdown files and an empty .gitkeep. No code execution, no user data, no network calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-16-01 | N/A | README.md | accept | Plain Markdown file; no executable content, no secrets, no user data. No threats applicable. |
</threat_model>

<verification>
1. README.md exists in repository root and does NOT contain the default Flutter placeholder text
2. README.md contains project description mentioning "Drinky Drinky", hydration tracking, and offline-first
3. README.md contains two Markdown image references pointing to docs/images/home_ios.png and docs/images/home_android.png
4. README.md contains numbered build instructions including flutter pub get, build_runner, and flutter run
5. README.md contains platform requirements (compileSdk 36, minSdk 26, iOS 13+)
6. docs/images/ directory exists with .gitkeep
</verification>

<success_criteria>
- DOC-01 satisfied: README.md describes the project with two home screen screenshots (iOS and Android) and essential build instructions
- A developer with Flutter installed can follow the README to clone and run the app
- Screenshot paths are correct so images render on GitHub once the developer adds the PNG files
</success_criteria>

<output>
Create `.planning/phases/16-project-readme/16-01-SUMMARY.md` when done
</output>
