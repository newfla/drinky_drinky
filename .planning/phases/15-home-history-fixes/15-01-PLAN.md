---
phase: 15-home-history-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/screens/home_screen.dart
  - lib/data/database/daos/water_entry_dao.dart
  - lib/data/repositories/water_repository.dart
  - lib/core/providers/stream_providers.dart
  - lib/core/providers/stream_providers.g.dart
  - lib/presentation/screens/history_screen.dart
autonomous: true
requirements:
  - POLISH-01
  - BUG-04

must_haves:
  truths:
    - "Home screen placeholder text ('noDrinksLogged' and 'noDrinksLoggedHint') is horizontally centered with 32px lateral padding"
    - "On a fresh install, after logging the first water intake on the home tab, navigating to the history tab shows today's intake data (not 'Nessuna cronologia' / empty state)"
    - "After adding multiple intakes on a fresh install, the history screen timeline updates to reflect all entries without requiring app restart"
  artifacts:
    - path: "lib/presentation/screens/home_screen.dart"
      provides: "Centered empty state with horizontal padding"
      contains: "EdgeInsets.symmetric(horizontal: 32)"
    - path: "lib/data/database/daos/water_entry_dao.dart"
      provides: "Streaming MIN(date_key) aggregate query"
      contains: "watchEarliestDateKey"
    - path: "lib/data/repositories/water_repository.dart"
      provides: "Repository pass-through for watchEarliestDateKey"
      contains: "watchEarliestDateKey"
    - path: "lib/core/providers/stream_providers.dart"
      provides: "earliestDateKey stream provider"
      contains: "earliestDateKey"
    - path: "lib/presentation/screens/history_screen.dart"
      provides: "Reactive history screen consuming earliestDateKeyProvider"
      contains: "earliestDateKeyProvider"
  key_links:
    - from: "lib/data/database/daos/water_entry_dao.dart"
      to: "water_entries table"
      via: "selectOnly + min() + watchSingle()"
      pattern: "waterEntries\\.dateKey\\.min\\(\\)"
    - from: "lib/core/providers/stream_providers.dart"
      to: "lib/data/repositories/water_repository.dart"
      via: "ref.watch(waterRepositoryProvider)"
      pattern: "repo\\.watchEarliestDateKey\\(\\)"
    - from: "lib/presentation/screens/history_screen.dart"
      to: "lib/core/providers/stream_providers.dart"
      via: "ref.watch(earliestDateKeyProvider)"
      pattern: "ref\\.watch\\(earliestDateKeyProvider\\)"
---

<objective>
Fix two visible UI issues on existing screens: center the home screen empty-state placeholder text with consistent padding (POLISH-01, D-01, D-02), and make the history screen reactively update when the first water intake is logged on a fresh install (BUG-04, D-03, D-04, D-05).

Purpose: After this plan, both screens display correct states from the very first user interaction -- no stale empty states, no off-center text.
Output: Modified home_screen.dart, water_entry_dao.dart, water_repository.dart, stream_providers.dart (+ regenerated .g.dart), and refactored history_screen.dart.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-home-history-fixes/15-CONTEXT.md
@.planning/phases/15-home-history-fixes/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix home screen empty-state text centering and padding (POLISH-01)</name>
  <files>lib/presentation/screens/home_screen.dart</files>
  <read_first>
    - lib/presentation/screens/home_screen.dart (current _buildEmptyState at lines 201-219 -- the fix target)
    - lib/presentation/screens/history_screen.dart (lines 86-112 -- the reference pattern with Padding(all: 32) + textAlign: TextAlign.center to match for visual consistency)
  </read_first>
  <action>
Modify `_buildEmptyState` in home_screen.dart (lines 201-219) per D-01 and D-02:

1. Wrap the existing `Column` widget inside a `Padding` widget with `EdgeInsets.symmetric(horizontal: 32)` per D-01. The `Center` widget remains as the outermost wrapper, then `Padding`, then `Column`.

2. Add `textAlign: TextAlign.center` to the first `Text` widget (`context.l10n.noDrinksLogged`) per D-02. Currently this Text has no textAlign property.

3. Add `textAlign: TextAlign.center` to the second `Text` widget (`context.l10n.noDrinksLoggedHint`) per D-02. Currently this Text has no textAlign property.

The result is: `Center` > `Padding(horizontal: 32)` > `Column(mainAxisSize: min)` > two `Text` widgets both with `textAlign: TextAlign.center`. This matches the pattern used in the history screen empty state (lines 86-112 of history_screen.dart) for visual consistency across the app.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && grep -c 'EdgeInsets.symmetric(horizontal: 32)' lib/presentation/screens/home_screen.dart | grep -q '^1$' && grep -c 'textAlign: TextAlign.center' lib/presentation/screens/home_screen.dart | grep -q '^2$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - home_screen.dart _buildEmptyState contains `Padding` with `EdgeInsets.symmetric(horizontal: 32)` wrapping the `Column`
    - home_screen.dart contains exactly 2 occurrences of `textAlign: TextAlign.center` (one per Text widget in _buildEmptyState)
    - The widget tree order is Center > Padding > Column > Text widgets
    - `flutter analyze` reports no new errors on home_screen.dart
  </acceptance_criteria>
  <done>Home screen empty-state placeholder text has 32px horizontal padding and both text widgets use TextAlign.center, matching the history screen empty-state pattern</done>
</task>

<task type="auto">
  <name>Task 2: Add reactive earliest-date stream and refactor history screen (BUG-04)</name>
  <files>
    lib/data/database/daos/water_entry_dao.dart,
    lib/data/repositories/water_repository.dart,
    lib/core/providers/stream_providers.dart,
    lib/core/providers/stream_providers.g.dart,
    lib/presentation/screens/history_screen.dart
  </files>
  <read_first>
    - lib/data/database/daos/water_entry_dao.dart (existing watchTotalForDate at lines 26-32 -- the pattern to follow for watchEarliestDateKey; also getEarliestDateKey at lines 63-69 -- the Future-based predecessor)
    - lib/data/repositories/water_repository.dart (existing watchTotalForDate at line 62-64 -- pass-through pattern; also getEarliestDateKey at lines 85-86)
    - lib/core/providers/stream_providers.dart (existing totalMlForDate at lines 36-40 -- the @riverpod Stream pattern to follow)
    - lib/presentation/screens/history_screen.dart (full file -- understanding the initState BUG-04 root cause at lines 55-73, the _loading/_noEntries/_firstDay local state at lines 43-53, the empty state at lines 86-112, and the targetsAsync.when pattern at lines 121-307)
    - lib/core/router/app_router.dart (line 57 -- confirms StatefulShellRoute.indexedStack keeps HistoryScreen alive, root cause of BUG-04)
  </read_first>
  <action>
Four-file vertical slice per D-03, D-04, D-05:

**1. water_entry_dao.dart** -- Add `watchEarliestDateKey()` method to `WaterEntryDao` class per D-05. Use `selectOnly(waterEntries)` with `waterEntries.dateKey.min()` aggregate and `watchSingle()`. Map the result row via `row.read(minDateKey)` returning `Stream<String?>`. This follows the exact pattern of `watchTotalForDate` (lines 26-32) but uses `min()` instead of `sum()` and has no `where` clause. The method returns `null` when the table is empty (MIN on empty table yields SQL NULL). No codegen needed -- adding methods to a DAO class body does not affect the generated mixin.

**2. water_repository.dart** -- Add `watchEarliestDateKey()` method as a thin pass-through: `Stream<String?> watchEarliestDateKey() => _db.waterEntryDao.watchEarliestDateKey();`. Place it after the existing `getEarliestDateKey()` Future method (line 85-86). Follow the same delegation pattern as `watchTotalForDate` (line 62-64).

**3. stream_providers.dart** -- Add `earliestDateKey` provider function with `@riverpod` annotation (auto-dispose, no keepAlive -- per Claude's discretion, default auto-dispose is fine since StatefulShellRoute keeps the widget alive). The function signature: `Stream<String?> earliestDateKey(Ref ref)`. Body: `ref.watch(waterRepositoryProvider)` then `return repo.watchEarliestDateKey()`. Follow the pattern of `totalMlForDate` (lines 36-40) but without parameters. Then run `dart run build_runner build --delete-conflicting-outputs` to regenerate `stream_providers.g.dart` which will produce the `earliestDateKeyProvider` symbol.

**4. history_screen.dart** -- Full refactor per D-04:
  - Remove the three local state variables: `_firstDay`, `_loading`, `_noEntries` (lines 43-53).
  - Remove the entire `initState` override (lines 54-73) -- `_selectedDay` can be initialized inline at declaration (`DateTime? _selectedDay;` is already sufficient since it defaults to null).
  - Keep only `_selectedDay` as local widget state.
  - In `build()`, add `final earliestAsync = ref.watch(earliestDateKeyProvider);` at the top.
  - Replace the current `if (_loading)` and `if (_noEntries)` early returns (lines 78-112) and the main `targetsAsync.when()` body (lines 121-307) with a single outer `earliestAsync.when()`:
    - `loading:` returns `Center(child: CircularProgressIndicator())` matching existing pattern.
    - `error:` returns `Center(child: Text(context.l10n.errorLoadingData))` matching existing pattern.
    - `data: (earliestDateKey)` -- if `earliestDateKey == null`, return the existing `_buildEmptyState(context)` call (the empty state UI at lines 86-112 can be extracted to a method or kept inline). If non-null, parse it to `DateTime` via `DateTime.parse(earliestDateKey)` and use this as the local `firstDay` variable. Then nest the existing `targetsAsync.when()` inside the data branch.
  - Replace all references to `_firstDay!` with the local `firstDay` variable (lines 145-146, 189).
  - The Scaffold with AppBar wraps the entire `earliestAsync.when()` as body.
  - Remove the `import '../../core/providers/repository_providers.dart'` if it becomes unused after removing the `ref.read(waterRepositoryProvider)` call from the deleted `initState`. Check whether any other code in the file still uses it.
  </action>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && flutter analyze lib/data/database/daos/water_entry_dao.dart lib/data/repositories/water_repository.dart lib/core/providers/stream_providers.dart lib/presentation/screens/history_screen.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - water_entry_dao.dart contains method `Stream<String?> watchEarliestDateKey()` using `waterEntries.dateKey.min()` + `selectOnly` + `watchSingle()`
    - water_repository.dart contains method `Stream<String?> watchEarliestDateKey()` delegating to `_db.waterEntryDao.watchEarliestDateKey()`
    - stream_providers.dart contains `@riverpod` annotated function `Stream<String?> earliestDateKey(Ref ref)` using `repo.watchEarliestDateKey()`
    - stream_providers.g.dart contains generated `earliestDateKeyProvider`
    - history_screen.dart does NOT contain `_firstDay`, `_noEntries`, or `_loading` field declarations
    - history_screen.dart does NOT contain `initState` override
    - history_screen.dart contains `ref.watch(earliestDateKeyProvider)` 
    - history_screen.dart contains `earliestAsync.when(` pattern
    - history_screen.dart still contains `_selectedDay` as local state
    - `flutter analyze` reports no errors on all 4 modified source files
    - `dart run build_runner build` completes without errors
  </acceptance_criteria>
  <done>History screen reactively updates when water entries are added or removed, eliminating the stale empty-state bug on fresh install. The screen uses the same provider-first reactive pattern as HomeScreen.</done>
</task>

</tasks>

## Artifacts this phase produces

### New methods
- `WaterEntryDao.watchEarliestDateKey()` -- `Stream<String?>` method in `lib/data/database/daos/water_entry_dao.dart`
- `WaterRepository.watchEarliestDateKey()` -- `Stream<String?>` method in `lib/data/repositories/water_repository.dart`

### New providers
- `earliestDateKey()` -- `@riverpod Stream<String?>` function in `lib/core/providers/stream_providers.dart`
- `earliestDateKeyProvider` -- generated provider in `lib/core/providers/stream_providers.g.dart`

### Modified widgets
- `_buildEmptyState()` in `HomeScreen` -- added `Padding` wrapper and `textAlign` properties
- `HistoryScreen` -- removed `_firstDay`, `_noEntries`, `_loading` local state; removed `initState` override; added `ref.watch(earliestDateKeyProvider)` with `AsyncValue.when()` consumption

### Removed symbols
- `_HistoryScreenState._firstDay` field
- `_HistoryScreenState._noEntries` field
- `_HistoryScreenState._loading` field
- `_HistoryScreenState.initState()` override

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase modifies internal UI layout and replaces a Future-based data fetch with a Stream-based fetch of the same data from the same local SQLite database.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | watchEarliestDateKey() | accept | Returns MIN(date_key) from local SQLite -- same data already exposed via getEarliestDateKey() Future. No new data surface. Single-user local app, no network exposure. |

No package installs in this phase -- supply chain gate not applicable.
</threat_model>

<verification>
1. `flutter analyze` passes with no errors on all modified files
2. `dart run build_runner build` succeeds and generates `earliestDateKeyProvider` in stream_providers.g.dart
3. App launches without errors on iOS simulator or Android emulator
4. Home screen empty state shows centered text with horizontal padding when no intakes exist
5. On fresh install: log first intake on home tab, switch to history tab -- history shows calendar with today's data (not empty state)
</verification>

<success_criteria>
- POLISH-01: Home screen placeholder text has 32px horizontal padding and center-aligned text, matching history screen empty-state visual pattern
- BUG-04: History screen reactively shows intake data after first entry on fresh install without requiring app restart or tab re-creation
- All modified files pass `flutter analyze` with no errors
- Code generation succeeds for the new `earliestDateKeyProvider`
</success_criteria>

<output>
Create `.planning/phases/15-home-history-fixes/15-01-SUMMARY.md` when done
</output>
