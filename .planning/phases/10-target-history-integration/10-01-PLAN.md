---
phase: 10-target-history-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/database/tables/user_settings_table.dart
  - lib/data/database/daos/target_history_dao.dart
  - lib/data/database/daos/user_settings_dao.dart
  - lib/domain/entities/user_settings_entity.dart
  - lib/data/repositories/settings_repository.dart
autonomous: true
requirements: [BUG-02, TARGET-02]

must_haves:
  truths:
    - "TargetHistoryDao has a watchTargetForDate(dateKey) stream method returning the effective target for any date"
    - "UserSettings table includes applyFromTomorrow boolean column with default false"
    - "UserSettingsEntity includes applyFromTomorrow field and copyWith support"
    - "SettingsRepository has updateTargetWithHistory(int newTargetMl) that dual-writes to target_history and user_settings"
    - "All entity mappings in SettingsRepository include applyFromTomorrow"
  artifacts:
    - path: "lib/data/database/daos/target_history_dao.dart"
      provides: "watchTargetForDate stream method"
      contains: "watchTargetForDate"
    - path: "lib/data/database/tables/user_settings_table.dart"
      provides: "applyFromTomorrow column definition"
      contains: "applyFromTomorrow"
    - path: "lib/domain/entities/user_settings_entity.dart"
      provides: "applyFromTomorrow field on freezed entity"
      contains: "applyFromTomorrow"
    - path: "lib/data/repositories/settings_repository.dart"
      provides: "updateTargetWithHistory method with dual-write logic"
      contains: "updateTargetWithHistory"
  key_links:
    - from: "lib/data/repositories/settings_repository.dart"
      to: "lib/data/database/daos/target_history_dao.dart"
      via: "insertOrReplace call in updateTargetWithHistory"
      pattern: "targetHistoryDao\\.insertOrReplace"
    - from: "lib/data/repositories/settings_repository.dart"
      to: "lib/data/database/daos/user_settings_dao.dart"
      via: "updateSettings call in updateTargetWithHistory"
      pattern: "userSettingsDao\\.updateSettings"
---

<objective>
Extend the data layer to support per-day target history integration: add watchTargetForDate() stream to TargetHistoryDao (per D-08), add applyFromTomorrow column to UserSettings (per D-05), propagate the new field through entity and repository mappings, and add updateTargetWithHistory() dual-write method to SettingsRepository (per D-06). Run code-gen to regenerate Drift and freezed outputs.

Purpose: The provider and UI layers (Plans 02 and 03) depend on these data-layer primitives. Without watchTargetForDate(), the family provider cannot stream per-day targets. Without updateTargetWithHistory(), settings cannot route target changes through target_history. Without applyFromTomorrow, the today/tomorrow toggle has no persistence.

Output: Modified DAO, table, entity, and repository files with regenerated code. All compile with zero dart analyze issues.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-target-history-integration/10-CONTEXT.md
@.planning/phases/10-target-history-integration/10-RESEARCH.md
@.planning/phases/10-target-history-integration/10-PATTERNS.md
@.planning/phases/09-data-foundation-bug-fixes/09-01-SUMMARY.md
</context>

## Artifacts this phase produces

### Plan 01 artifacts (this plan)
- **Method** `watchTargetForDate(String dateKey)` on `TargetHistoryDao` -- returns `Stream<int?>`, uses `.watchSingleOrNull()` on the same query as `getTargetForDate`
- **Column** `applyFromTomorrow` on `UserSettings` Drift table -- `BoolColumn`, `withDefault(const Constant(false))`
- **Field** `applyFromTomorrow` on `UserSettingsEntity` freezed class -- `required bool applyFromTomorrow`
- **Method** `updateTargetWithHistory(int newTargetMl)` on `SettingsRepository` -- reads applyFromTomorrow, computes effectiveDate, calls insertOrReplace + updateSettings
- **Updated default** `applyFromTomorrow: false` in `UserSettingsDao._defaultSettings()`
- **Updated mappings** `applyFromTomorrow` in `SettingsRepository.watchSettings()`, `getSettings()`, `updateSettings()`

<tasks>

<task type="auto">
  <name>Task 1: Schema extension and DAO stream method</name>
  <files>
    lib/data/database/tables/user_settings_table.dart,
    lib/data/database/daos/target_history_dao.dart,
    lib/data/database/daos/user_settings_dao.dart
  </files>
  <read_first>
    lib/data/database/tables/user_settings_table.dart,
    lib/data/database/daos/target_history_dao.dart,
    lib/data/database/daos/user_settings_dao.dart
  </read_first>
  <action>
    1. In user_settings_table.dart, add a new column after the existing `dndEnabled` column (per D-05):
       - Name: `applyFromTomorrow`
       - Type: `BoolColumn`
       - Default: `withDefault(const Constant(false))`
       - Same pattern as the `dndEnabled` column at lines 17-18

    2. In target_history_dao.dart, add a new public method `watchTargetForDate(String dateKey)` returning `Stream<int?>` (per D-08). The method uses the same SELECT query as `getTargetForDate` (lines 14-21) but replaces `.getSingleOrNull()` with `.watchSingleOrNull()` and maps the result to `row?.targetMl`. Drop `async` keyword since it returns a stream, not a future. Follow the watchSingleOrNull pattern from user_settings_dao.dart lines 16-19.

    3. In user_settings_dao.dart, update `_defaultSettings()` (lines 33-44) to include `applyFromTomorrow: false` after the `dndEnabled: true` field. This ensures the generated `UserSetting` data class default is consistent.
  </action>
  <acceptance_criteria>
    - user_settings_table.dart contains `BoolColumn get applyFromTomorrow` with `withDefault(const Constant(false))`
    - target_history_dao.dart contains method `Stream<int?> watchTargetForDate(String dateKey)` using `.watchSingleOrNull()` and `.map((row) => row?.targetMl)`
    - user_settings_dao.dart `_defaultSettings()` includes `applyFromTomorrow: false`
    - No `async` keyword on `watchTargetForDate` method signature
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && grep -c "applyFromTomorrow" lib/data/database/tables/user_settings_table.dart && grep -c "watchTargetForDate" lib/data/database/daos/target_history_dao.dart && grep -c "applyFromTomorrow" lib/data/database/daos/user_settings_dao.dart</automated>
  </verify>
  <done>UserSettings table has applyFromTomorrow column, TargetHistoryDao has watchTargetForDate stream method, UserSettingsDao default includes applyFromTomorrow</done>
</task>

<task type="auto">
  <name>Task 2: Entity, repository mappings, updateTargetWithHistory, and code-gen</name>
  <files>
    lib/domain/entities/user_settings_entity.dart,
    lib/data/repositories/settings_repository.dart
  </files>
  <read_first>
    lib/domain/entities/user_settings_entity.dart,
    lib/data/repositories/settings_repository.dart,
    lib/data/database/daos/target_history_dao.dart
  </read_first>
  <action>
    1. In user_settings_entity.dart, add `required bool applyFromTomorrow` as the last field in the freezed factory constructor, after `required bool dndEnabled` (line 14). This follows the existing field pattern.

    2. In settings_repository.dart, update ALL THREE entity mappings to include `applyFromTomorrow`:
       - `watchSettings()` mapping (line 14-22): add `applyFromTomorrow: row.applyFromTomorrow`
       - `getSettings()` mapping (line 29-36): add `applyFromTomorrow: row.applyFromTomorrow`
       - `updateSettings()` companion (line 61-69): add `applyFromTomorrow: Value(entity.applyFromTomorrow)`

    3. In settings_repository.dart, add a new public method `updateTargetWithHistory(int newTargetMl)` returning `Future<void>` (per D-06). The method:
       - Throws `ArgumentError` if `newTargetMl <= 0` (input validation, same pattern as existing `updateSettings`)
       - Calls `await _db.userSettingsDao.getSettings()` to read current settings
       - Checks `currentSettings.applyFromTomorrow` to decide effectiveDate
       - If true, effectiveDate = tomorrow formatted as YYYY-MM-DD; if false, effectiveDate = today formatted as YYYY-MM-DD
       - Calls `await _db.targetHistoryDao.insertOrReplace(effectiveDate, newTargetMl)` for the upsert (per D-07)
       - Calls `await _db.userSettingsDao.updateSettings(UserSettingsCompanion(dailyTargetMl: Value(newTargetMl)))` to keep dailyTargetMl in sync
       - Requires import of `Value` from drift (already imported at line 1)

    4. Run code generation: `dart run build_runner build --delete-conflicting-outputs`
       This regenerates: app_database.g.dart, target_history_dao.g.dart, user_settings_dao.g.dart, user_settings_entity.freezed.dart, repository_providers.g.dart

    5. Run `dart analyze` to confirm zero issues.
  </action>
  <acceptance_criteria>
    - user_settings_entity.dart contains `required bool applyFromTomorrow` in factory constructor
    - settings_repository.dart watchSettings mapping contains `applyFromTomorrow: row.applyFromTomorrow`
    - settings_repository.dart getSettings mapping contains `applyFromTomorrow: row.applyFromTomorrow`
    - settings_repository.dart updateSettings companion contains `applyFromTomorrow: Value(entity.applyFromTomorrow)`
    - settings_repository.dart contains `Future<void> updateTargetWithHistory(int newTargetMl)` method
    - updateTargetWithHistory calls `_db.targetHistoryDao.insertOrReplace` and `_db.userSettingsDao.updateSettings`
    - `dart run build_runner build --delete-conflicting-outputs` exits 0
    - `dart analyze` reports no issues
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart analyze 2>&1 | tail -5</automated>
  </verify>
  <done>Entity has applyFromTomorrow field, repository has all three mappings updated, updateTargetWithHistory method exists with dual-write logic, code-gen and analyze pass clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI -> Repository | User-provided target value crosses into persistence layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | updateTargetWithHistory | mitigate | Validate newTargetMl > 0 with ArgumentError before any writes |
| T-10-02 | Tampering | applyFromTomorrow column | accept | Boolean column with default false; SQLite enforces type; low-risk local app |
</threat_model>

<verification>
- `dart analyze` reports zero issues after code-gen
- `grep -c "watchTargetForDate" lib/data/database/daos/target_history_dao.dart` returns >= 1
- `grep -c "updateTargetWithHistory" lib/data/repositories/settings_repository.dart` returns >= 1
- `grep -c "applyFromTomorrow" lib/domain/entities/user_settings_entity.dart` returns >= 1
- Existing tests still pass: `flutter test test/data/database/daos/target_history_dao_test.dart`
</verification>

<success_criteria>
- watchTargetForDate stream method exists on TargetHistoryDao returning Stream<int?>
- applyFromTomorrow column exists on UserSettings table with default false
- UserSettingsEntity includes applyFromTomorrow in freezed factory
- All three SettingsRepository mappings (watch, get, update) include applyFromTomorrow
- updateTargetWithHistory dual-write method exists on SettingsRepository
- Code-gen passes, dart analyze clean, existing tests pass
</success_criteria>

<output>
Create `.planning/phases/10-target-history-integration/10-01-SUMMARY.md` when done
</output>
