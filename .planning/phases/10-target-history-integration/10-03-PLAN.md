---
phase: 10-target-history-integration
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - lib/presentation/screens/home_screen.dart
  - lib/presentation/screens/history_screen.dart
  - lib/presentation/screens/settings_screen.dart
autonomous: true
requirements: [BUG-02, TARGET-02, TARGET-03, TARGET-04]

must_haves:
  truths:
    - "Home screen progress ring and goal text use per-day target from effectiveTargetForDateProvider, not settings.dailyTargetMl"
    - "Home screen date key comes from todayDateKeyProvider, not manual Timer.periodic polling"
    - "Calendar green/red decorations evaluate each day against that day's effective target from allTargetHistoryProvider"
    - "Settings screen has an 'Applica da domani' toggle that persists applyFromTomorrow preference"
    - "Settings target slider routes changes through updateTargetWithHistory, not bare updateSettings"
    - "Goal-reached notification cancel uses per-day target, not settings.dailyTargetMl"
  artifacts:
    - path: "lib/presentation/screens/home_screen.dart"
      provides: "Midnight auto-reset via todayDateKeyProvider, per-day target in progress ring"
      contains: "todayDateKeyProvider"
    - path: "lib/presentation/screens/history_screen.dart"
      provides: "Per-day target evaluation in calendar day builders"
      contains: "allTargetHistoryProvider"
    - path: "lib/presentation/screens/settings_screen.dart"
      provides: "applyFromTomorrow toggle and updateTargetWithHistory routing"
      contains: "updateTargetWithHistory"
  key_links:
    - from: "lib/presentation/screens/home_screen.dart"
      to: "lib/core/providers/stream_providers.dart"
      via: "ref.watch(todayDateKeyProvider) and ref.watch(effectiveTargetForDateProvider)"
      pattern: "ref\\.watch\\(todayDateKeyProvider\\)"
    - from: "lib/presentation/screens/history_screen.dart"
      to: "lib/core/providers/stream_providers.dart"
      via: "ref.watch(allTargetHistoryProvider) for batch target lookup"
      pattern: "ref\\.watch\\(allTargetHistoryProvider\\)"
    - from: "lib/presentation/screens/settings_screen.dart"
      to: "lib/data/repositories/settings_repository.dart"
      via: "ref.read(settingsRepositoryProvider).updateTargetWithHistory()"
      pattern: "updateTargetWithHistory"
---

<objective>
Wire all three screens to the per-day target providers and midnight reset: HomeScreen uses todayDateKeyProvider + effectiveTargetForDateProvider (per D-02, D-09, fixing BUG-02 + TARGET-03), HistoryScreen uses allTargetHistoryProvider for per-day calendar coloring (per D-10, TARGET-04), and SettingsScreen adds the applyFromTomorrow toggle and routes target changes through updateTargetWithHistory (per D-04, D-06, TARGET-02).

Purpose: This is the user-facing deliverable of Phase 10. Users will see correct per-day targets in the progress ring, accurate green/red calendar decorations based on historical targets, and a toggle to control when target changes take effect.

Output: Three modified screen files. The app displays per-day targets, auto-resets at midnight, and provides today/tomorrow target application control.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-target-history-integration/10-CONTEXT.md
@.planning/phases/10-target-history-integration/10-RESEARCH.md
@.planning/phases/10-target-history-integration/10-PATTERNS.md
@.planning/phases/10-target-history-integration/10-01-SUMMARY.md
@.planning/phases/10-target-history-integration/10-02-SUMMARY.md
</context>

## Artifacts this phase produces

### Plan 03 artifacts (this plan)
- **Modified widget** `HomeScreen` -- removes `_dateKey` field, `_midnightTimer`, `_checkDateChange()`, `Timer.periodic`; uses `ref.watch(todayDateKeyProvider)` for date key and `ref.watch(effectiveTargetForDateProvider(todayKey))` for target
- **Modified widget** `HistoryScreen` -- watches `allTargetHistoryProvider` instead of `userSettingsProvider` for target; uses `_findActiveTarget()` helper for per-day lookup in calendar builders and day summary
- **Helper function** `_findActiveTarget(List<TargetHistoryData> targets, String dateKey)` in history_screen.dart -- linear scan through sorted targets to find active target for a date
- **Modified widget** `SettingsScreen` -- adds `SwitchListTile` for applyFromTomorrow toggle in daily goal card; replaces `updateSettings(settings.copyWith(dailyTargetMl: ...))` with `updateTargetWithHistory(val.toInt())`

<tasks>

<task type="auto">
  <name>Task 1: HomeScreen migration to todayDateKeyProvider and per-day target</name>
  <files>lib/presentation/screens/home_screen.dart</files>
  <read_first>
    lib/presentation/screens/home_screen.dart,
    lib/core/providers/stream_providers.dart
  </read_first>
  <action>
    Migrate HomeScreen from manual Timer.periodic midnight polling to reactive todayDateKeyProvider, and from global settings.dailyTargetMl to per-day effectiveTargetForDateProvider.

    1. REMOVE from _HomeScreenState (per D-01, D-02):
       - Field `late String _dateKey` (line 23)
       - Field `Timer? _midnightTimer` (line 25)
       - Assignment `_dateKey = todayDateKey()` from initState (line 30)
       - The `_midnightTimer = Timer.periodic(const Duration(seconds: 60), (_) => _checkDateChange())` call from initState (lines 39-42)
       - The `_checkDateChange()` method entirely (lines 51-56)
       - The `_midnightTimer?.cancel()` from dispose (line 61)
       - The `import 'dart:async';` at line 1 (Timer no longer used in this file)

    2. KEEP in _HomeScreenState:
       - `late AppLifecycleListener _lifecycleListener` (line 24)
       - The `_lifecycleListener` setup in initState -- but modify the onResume callback to only call `_rescheduleNotifications()` (remove `_checkDateChange()` call at line 34). Date change is now handled reactively by todayDateKeyProvider.
       - `_lifecycleListener.dispose()` in dispose (line 60)

    3. UPDATE build() method (per D-02, D-09):
       - Add at top of build: `final todayKey = ref.watch(todayDateKeyProvider);`
       - Replace `totalMlForDateProvider(_dateKey)` with `totalMlForDateProvider(todayKey)` (line 68)
       - Replace `waterEntriesForDateProvider(_dateKey)` with `waterEntriesForDateProvider(todayKey)` (line 69)

    4. UPDATE ref.listen for goal-reached notification cancel (lines 77-88, per D-09 and Pitfall 6 from RESEARCH.md):
       - Change the provider being listened to: `totalMlForDateProvider(todayKey)` instead of `totalMlForDateProvider(_dateKey)`
       - Change target source: replace `ref.read(userSettingsProvider).value?.dailyTargetMl ?? 0` (line 83) with `ref.read(effectiveTargetForDateProvider(todayKey)).value ?? 0`

    5. UPDATE _buildContent (per D-09):
       - Remove `final target = settings.dailyTargetMl;` (line 129)
       - The target must come from the provider. Add parameter `int target` to `_buildContent` signature (replacing the implicit `settings.dailyTargetMl` usage)
       - In the build() data callback (line 117), pass the target: compute `final target = ref.watch(effectiveTargetForDateProvider(todayKey)).value ?? 2000;` and pass it to `_buildContent(context, settings, totalMl, entries, target)`
       - Update _buildContent signature to accept `int target` as fifth parameter
       - The rest of _buildContent already uses `target` variable -- no further changes needed

    6. UPDATE _onQuickAdd (per D-02):
       - Replace `final capturedKey = _dateKey;` (line 246) with `final capturedKey = ref.read(todayDateKeyProvider);`
  </action>
  <acceptance_criteria>
    - home_screen.dart does NOT contain `_dateKey` field declaration
    - home_screen.dart does NOT contain `_midnightTimer` field declaration
    - home_screen.dart does NOT contain `Timer.periodic`
    - home_screen.dart does NOT contain `_checkDateChange` method
    - home_screen.dart contains `ref.watch(todayDateKeyProvider)` in build()
    - home_screen.dart contains `ref.watch(effectiveTargetForDateProvider(todayKey))` for target
    - home_screen.dart ref.listen uses `effectiveTargetForDateProvider` for target comparison, not `userSettingsProvider`
    - home_screen.dart _onQuickAdd uses `ref.read(todayDateKeyProvider)` not `_dateKey`
    - `dart analyze` reports no issues
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart analyze lib/presentation/screens/home_screen.dart 2>&1 | tail -3</automated>
  </verify>
  <done>HomeScreen uses todayDateKeyProvider for reactive midnight reset (BUG-02 fixed), effectiveTargetForDateProvider for per-day target (TARGET-03), no more Timer.periodic polling</done>
</task>

<task type="auto">
  <name>Task 2: HistoryScreen per-day target and SettingsScreen toggle + updateTargetWithHistory</name>
  <files>
    lib/presentation/screens/history_screen.dart,
    lib/presentation/screens/settings_screen.dart
  </files>
  <read_first>
    lib/presentation/screens/history_screen.dart,
    lib/presentation/screens/settings_screen.dart,
    lib/core/providers/stream_providers.dart,
    lib/data/repositories/settings_repository.dart,
    lib/domain/entities/user_settings_entity.dart
  </read_first>
  <action>
    **HistoryScreen changes (per D-03, D-10):**

    1. Add import for TargetHistoryData type: `import '../../data/database/app_database.dart';` (needed for the type in _findActiveTarget helper).

    2. Add a file-level helper function `_findActiveTarget` (place after the existing `_monthName` function, before the `HistoryScreen` class):
       - Signature: `int _findActiveTarget(List<TargetHistoryData> targets, String dateKey)`
       - Logic: initialize `result = 2000` (fallback). Iterate through `targets` (assumed sorted ASC by effectiveDate). For each `t`, if `t.effectiveDate.compareTo(dateKey) <= 0`, set `result = t.targetMl`. Else break. Return result.
       - Comment: "Finds the effective target for a date by scanning sorted target history. Returns 2000 as fallback if no targets exist."

    3. In build() method, replace the `userSettingsProvider` watch (line 117: `final settingsAsync = ref.watch(userSettingsProvider)`) with `final targetsAsync = ref.watch(allTargetHistoryProvider)`. The screen no longer needs global settings for target -- only for the calendar coloring.

    4. Update the settingsAsync.when block (starting line 121). Replace it with `targetsAsync.when(...)`. In the `data` callback:
       - Parameter changes from `settings` to `targets` (a `List<TargetHistoryData>`)
       - Remove `final dailyTarget = settings.dailyTargetMl;` (line 127)

    5. In the defaultBuilder callback (lines 225-249):
       - After `final total = monthTotals[dateKey];` (line 229), add: `final dailyTarget = _findActiveTarget(targets, dateKey);`
       - The existing `total >= dailyTarget` check (line 232) and `total < dailyTarget` check (line 240) now use the per-day target instead of the global one

    6. In the todayBuilder callback (lines 251-286):
       - After `final total = monthTotals[dateKey];` (line 253), add: `final dailyTarget = _findActiveTarget(targets, dateKey);`
       - The existing `total >= dailyTarget` check (line 263) now uses the per-day target

    7. In _buildDaySummary (lines 379-406):
       - Change parameter from `int dailyTarget` to `List<TargetHistoryData> targets`
       - Inside, compute: `final dailyTarget = _findActiveTarget(targets, dateKey);`
       - Update the caller (line 298-303): pass `targets` instead of `dailyTarget`

    8. Per D-03: The calendar does NOT use todayDateKeyProvider for midnight reset -- users navigate months explicitly. No change needed for the focused month / page change behavior.

    **SettingsScreen changes (per D-04, D-06):**

    1. In `_dailyGoalCard`, replace the slider's `onChangeEnd` callback (lines 112-117):
       - Remove: `ref.read(settingsRepositoryProvider).updateSettings(settings.copyWith(dailyTargetMl: val.toInt()))`
       - Replace with: `ref.read(settingsRepositoryProvider).updateTargetWithHistory(val.toInt())`
       - Keep the `setState(() => _dailyTargetDrag = null)` call (line 113)

    2. In `_dailyGoalCard`, add a `SwitchListTile` for the applyFromTomorrow toggle AFTER the Slider widget and before the closing of the Column children list (per D-04):
       - Add `const Divider()` as visual separator
       - SwitchListTile with:
         - title: `Text('Applica da domani')` (Italian, matching app convention)
         - subtitle: conditional text -- if `settings.applyFromTomorrow` is true, show `'Le modifiche al target entrano in vigore domani'`; if false, show `'Le modifiche al target entrano in vigore oggi'`
         - value: `settings.applyFromTomorrow`
         - onChanged: `(val) { ref.read(settingsRepositoryProvider).updateSettings(settings.copyWith(applyFromTomorrow: val)); }`
       - Pattern follows the existing DND SwitchListTile at settings_screen.dart lines 232-246

    3. Run `dart analyze` on both files to confirm zero issues.
  </action>
  <acceptance_criteria>
    - history_screen.dart contains `_findActiveTarget` helper function
    - history_screen.dart watches `allTargetHistoryProvider` instead of `userSettingsProvider` for target data
    - history_screen.dart defaultBuilder computes per-day target via `_findActiveTarget(targets, dateKey)`
    - history_screen.dart todayBuilder computes per-day target via `_findActiveTarget(targets, dateKey)`
    - history_screen.dart _buildDaySummary uses `_findActiveTarget` for per-day target
    - history_screen.dart does NOT contain `settings.dailyTargetMl` for calendar coloring
    - settings_screen.dart onChangeEnd calls `updateTargetWithHistory` not `updateSettings(settings.copyWith(dailyTargetMl: ...))`
    - settings_screen.dart contains `SwitchListTile` with `'Applica da domani'` title
    - settings_screen.dart SwitchListTile value is `settings.applyFromTomorrow`
    - settings_screen.dart SwitchListTile onChanged calls `updateSettings(settings.copyWith(applyFromTomorrow: val))`
    - `dart analyze` reports no issues on both files
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart analyze lib/presentation/screens/history_screen.dart lib/presentation/screens/settings_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <done>HistoryScreen uses per-day targets for calendar green/red (TARGET-04), SettingsScreen has applyFromTomorrow toggle (TARGET-02) and routes target changes through updateTargetWithHistory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget -> Provider | UI reads reactive state from providers |
| Widget -> Repository | Settings toggle writes preference via repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | Settings slider value | mitigate | updateTargetWithHistory validates newTargetMl > 0 before writes (Plan 01) |
| T-10-06 | Information Disclosure | Calendar targets | accept | All data is local; no sensitive information in target history display |
</threat_model>

<verification>
- `dart analyze` reports zero issues on all three screen files
- `grep -c "todayDateKeyProvider" lib/presentation/screens/home_screen.dart` returns >= 1
- `grep -c "effectiveTargetForDateProvider" lib/presentation/screens/home_screen.dart` returns >= 1
- `grep -c "allTargetHistoryProvider" lib/presentation/screens/history_screen.dart` returns >= 1
- `grep -c "_findActiveTarget" lib/presentation/screens/history_screen.dart` returns >= 2 (definition + usage)
- `grep -c "updateTargetWithHistory" lib/presentation/screens/settings_screen.dart` returns >= 1
- `grep -c "applyFromTomorrow" lib/presentation/screens/settings_screen.dart` returns >= 2 (value + onChanged)
- `grep -c "Timer.periodic" lib/presentation/screens/home_screen.dart` returns 0 (removed)
- `grep -c "_midnightTimer" lib/presentation/screens/home_screen.dart` returns 0 (removed)
</verification>

<success_criteria>
- HomeScreen auto-resets at midnight via todayDateKeyProvider (BUG-02 fixed)
- HomeScreen progress ring shows per-day target from effectiveTargetForDateProvider (TARGET-03)
- HistoryScreen calendar colors each day against that day's effective target (TARGET-04)
- SettingsScreen has "Applica da domani" toggle persisting applyFromTomorrow (TARGET-02)
- SettingsScreen target slider uses updateTargetWithHistory for dual-write (TARGET-02)
- dart analyze clean on all three files
</success_criteria>

<output>
Create `.planning/phases/10-target-history-integration/10-03-SUMMARY.md` when done
</output>
