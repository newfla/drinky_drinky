---
phase: 10-target-history-integration
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/core/providers/stream_providers.dart
autonomous: true
requirements: [BUG-02, TARGET-03, TARGET-04]

must_haves:
  truths:
    - "todayDateKeyProvider is a keepAlive Notifier that automatically updates state at midnight without polling"
    - "effectiveTargetForDate provider returns a reactive stream of the per-day target for any dateKey"
    - "allTargetHistory provider streams all target_history rows sorted ASC for batch calendar lookups"
    - "streak provider evaluates each historical day against that day's effective target, not a single global target"
  artifacts:
    - path: "lib/core/providers/stream_providers.dart"
      provides: "todayDateKeyProvider, effectiveTargetForDate, allTargetHistory, updated streak"
      contains: "TodayDateKey"
    - path: "lib/core/providers/stream_providers.g.dart"
      provides: "Generated provider code for new providers"
      contains: "todayDateKeyProvider"
  key_links:
    - from: "lib/core/providers/stream_providers.dart"
      to: "lib/data/database/daos/target_history_dao.dart"
      via: "watchTargetForDate and watchAll calls"
      pattern: "targetHistoryDao\\.watchTargetForDate"
    - from: "lib/core/providers/stream_providers.dart"
      to: "lib/core/providers/database_provider.dart"
      via: "ref.watch(appDatabaseProvider) for DAO access"
      pattern: "ref\\.watch\\(appDatabaseProvider\\)"
---

<objective>
Create the Riverpod provider layer for Phase 10: todayDateKeyProvider (midnight auto-reset per D-01/D-02, fixing BUG-02), effectiveTargetForDateProvider (per-day target stream per D-08), allTargetHistoryProvider (batch lookup for calendar per D-10), and update the streak provider to use per-day targets instead of a single global target (per D-11). Run code-gen after adding providers.

Purpose: These providers are the reactive backbone connecting the data layer (Plan 01) to the UI layer (Plan 03). Without todayDateKeyProvider, BUG-02 remains unfixed. Without effectiveTargetForDate and allTargetHistory, screens cannot display per-day targets. Without the streak update, streak counts are calculated against the wrong target.

Output: Updated stream_providers.dart with 3 new providers and 1 updated provider, regenerated stream_providers.g.dart, zero dart analyze issues.
</objective>

<execution_context>
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flavio.bizzarri/repo/drinky_drinky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-target-history-integration/10-CONTEXT.md
@.planning/phases/10-target-history-integration/10-RESEARCH.md
@.planning/phases/10-target-history-integration/10-PATTERNS.md
@.planning/phases/10-target-history-integration/10-01-SUMMARY.md
</context>

## Artifacts this phase produces

### Plan 02 artifacts (this plan)
- **Class** `TodayDateKey extends _$TodayDateKey` -- keepAlive Notifier<String> with midnight Timer, `ref.onDispose` cleanup
- **Provider** `todayDateKeyProvider` -- generated from `TodayDateKey` class, keepAlive
- **Function** `effectiveTargetForDate(Ref ref, String dateKey)` -- keepAlive stream provider family returning `Stream<int>` via `watchTargetForDate`
- **Provider** `effectiveTargetForDateProvider` -- generated family provider
- **Function** `allTargetHistory(Ref ref)` -- keepAlive stream provider returning `Stream<List<TargetHistoryData>>` via `watchAll()`
- **Provider** `allTargetHistoryProvider` -- generated provider
- **Updated function** `streak(Ref ref)` -- now combines water totals stream with target history stream via `asyncExpand`, uses per-day target lookup

<tasks>

<task type="auto">
  <name>Task 1: Add todayDateKeyProvider and effectiveTargetForDate provider</name>
  <files>lib/core/providers/stream_providers.dart</files>
  <read_first>
    lib/core/providers/stream_providers.dart,
    lib/core/providers/database_provider.dart,
    lib/data/database/daos/target_history_dao.dart
  </read_first>
  <action>
    Add `import 'dart:async';` at the top of stream_providers.dart (needed for Timer).
    Add `import 'database_provider.dart';` if not already present (needed for appDatabaseProvider).
    Add import for the generated TargetHistoryData type: `import '../../data/database/app_database.dart';` (needed for the allTargetHistory provider return type).

    1. Add `TodayDateKey` class-based Notifier AFTER the existing `FocusedMonth` class (after line 133) per D-01:
       - Annotation: `@Riverpod(keepAlive: true)` -- same as FocusedMonth at line 126
       - Class: `TodayDateKey extends _$TodayDateKey`
       - Private field: `Timer? _midnightTimer`
       - `build()` method: calls `ref.onDispose(() => _midnightTimer?.cancel())` (pattern from database_provider.dart line 9), calls `_scheduleMidnightRefresh()`, returns `_computeTodayKey()`
       - `_computeTodayKey()`: same format logic as existing `todayDateKey()` at line 57-60 -- `'${now.year}-${now.month.toString().padLeft(2, '0')}-${now.day.toString().padLeft(2, '0')}'`
       - `_scheduleMidnightRefresh()`: cancels existing `_midnightTimer`, computes `nextMidnight = DateTime(now.year, now.month, now.day + 1)`, computes duration with +1 second buffer, creates `Timer(duration, _onMidnight)`
       - `_onMidnight()`: sets `state = _computeTodayKey()`, calls `_scheduleMidnightRefresh()` for next day

    2. Add `effectiveTargetForDate` stream family provider AFTER TodayDateKey class (per D-08):
       - Annotation: `@Riverpod(keepAlive: true)` -- same keepAlive pattern as userSettings at line 39
       - Function signature: `Stream<int> effectiveTargetForDate(Ref ref, String dateKey)`
       - Body: `ref.watch(appDatabaseProvider)` to get db, then `db.targetHistoryDao.watchTargetForDate(dateKey).map((targetMl) => targetMl ?? 2000)` -- fallback 2000 per D-08
       - Add a comment: "defensive fallback -- should never be null after seed"
       - Pattern follows calendarMonth provider at lines 68-76

    3. Add `allTargetHistory` stream provider AFTER effectiveTargetForDate (for batch calendar use per D-10, D-11):
       - Annotation: `@Riverpod(keepAlive: true)`
       - Function signature: `Stream<List<TargetHistoryData>> allTargetHistory(Ref ref)`
       - Body: `ref.watch(appDatabaseProvider).targetHistoryDao.watchAll()`
       - Pattern follows drinkPresets provider at lines 46-50
  </action>
  <acceptance_criteria>
    - stream_providers.dart contains `import 'dart:async';`
    - stream_providers.dart contains `class TodayDateKey extends _$TodayDateKey` with `@Riverpod(keepAlive: true)`
    - TodayDateKey has `Timer? _midnightTimer` field
    - TodayDateKey.build() calls `ref.onDispose` to cancel timer
    - TodayDateKey._scheduleMidnightRefresh() computes seconds until midnight + 1s buffer
    - stream_providers.dart contains `Stream<int> effectiveTargetForDate(Ref ref, String dateKey)` with `@Riverpod(keepAlive: true)`
    - effectiveTargetForDate calls `watchTargetForDate(dateKey)` and maps null to 2000
    - stream_providers.dart contains `Stream<List<TargetHistoryData>> allTargetHistory(Ref ref)` with `@Riverpod(keepAlive: true)`
    - allTargetHistory calls `watchAll()`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && grep -c "TodayDateKey" lib/core/providers/stream_providers.dart && grep -c "effectiveTargetForDate" lib/core/providers/stream_providers.dart && grep -c "allTargetHistory" lib/core/providers/stream_providers.dart</automated>
  </verify>
  <done>TodayDateKey Notifier with midnight Timer exists, effectiveTargetForDate family stream provider exists, allTargetHistory batch provider exists</done>
</task>

<task type="auto">
  <name>Task 2: Update streak provider for per-day targets and run code-gen</name>
  <files>lib/core/providers/stream_providers.dart</files>
  <read_first>
    lib/core/providers/stream_providers.dart,
    lib/data/database/daos/target_history_dao.dart
  </read_first>
  <action>
    1. Rewrite the `streak` provider (currently lines 83-119) per D-11. The updated provider must:
       - Keep the `@riverpod` annotation (not keepAlive -- streak can be recalculated)
       - Watch `appDatabaseProvider` to access `targetHistoryDao` directly (DAO access pattern from repository_providers.dart)
       - Use `db.targetHistoryDao.watchAll()` as the outer stream via `asyncExpand`
       - Inside the asyncExpand callback, receive `targets` (List<TargetHistoryData> sorted ASC by effectiveDate)
       - If targets is empty, yield stream of 0 and return
       - Map over `repo.watchDailyTotalsInRange('2020-01-01', yesterdayKey)` as the inner stream
       - For each `totals` map, iterate backward from yesterday:
         - For each day, find the active target by scanning `targets` list forward: the last entry where `t.effectiveDate.compareTo(key) <= 0` gives the activeTarget
         - Use `targets.first.targetMl` as the initial fallback (earliest target)
         - If `total >= activeTarget`, increment count and go to previous day
         - If `total < activeTarget`, break
       - Guard: if activeTarget <= 0, return 0
       - Compute `yesterdayKey` using `_toDateKey(yesterday)` (existing helper at line 11)

    2. Remove the `ref.watch(userSettingsProvider).value` dependency from streak -- it no longer uses the global settings target. Remove the early-return guards for `settings == null` and `dailyTarget <= 0` (replaced by the targets.isEmpty check and per-day activeTarget <= 0 check).

    3. Run code generation: `dart run build_runner build --delete-conflicting-outputs`
       This regenerates stream_providers.g.dart with the new providers.

    4. Run `dart analyze` to confirm zero issues.
  </action>
  <acceptance_criteria>
    - streak provider no longer contains `ref.watch(userSettingsProvider)` or `settings.dailyTargetMl`
    - streak provider contains `db.targetHistoryDao.watchAll()` call
    - streak provider uses `asyncExpand` to combine target history with daily totals streams
    - streak provider iterates through `targets` list to find per-day active target using `effectiveDate.compareTo(key) <= 0`
    - `dart run build_runner build --delete-conflicting-outputs` exits 0
    - `dart analyze` reports no issues
    - stream_providers.g.dart contains `todayDateKeyProvider`, `effectiveTargetForDateProvider`, `allTargetHistoryProvider`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/flavio.bizzarri/repo/drinky_drinky && dart analyze 2>&1 | tail -5</automated>
  </verify>
  <done>Streak provider uses per-day targets from target_history instead of global dailyTargetMl, all new providers code-gen'd, dart analyze clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timer -> Notifier state | System timer fires callback that mutates provider state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service | TodayDateKey Timer | accept | Timer duration is at most ~24 hours, well within limits; single timer instance |
| T-10-04 | Tampering | effectiveTargetForDate | accept | Read-only stream from local SQLite; fallback to 2000 prevents null crashes |
</threat_model>

<verification>
- `dart analyze` reports zero issues after code-gen
- `grep -c "todayDateKeyProvider" lib/core/providers/stream_providers.g.dart` returns >= 1
- `grep -c "effectiveTargetForDateProvider" lib/core/providers/stream_providers.g.dart` returns >= 1
- `grep -c "allTargetHistoryProvider" lib/core/providers/stream_providers.g.dart` returns >= 1
- `grep -c "asyncExpand" lib/core/providers/stream_providers.dart` returns >= 1 (streak uses combined streams)
</verification>

<success_criteria>
- todayDateKeyProvider Notifier exists with midnight Timer and ref.onDispose cleanup
- effectiveTargetForDateProvider family stream provider exists returning Stream<int> with 2000 fallback
- allTargetHistoryProvider exists returning Stream<List<TargetHistoryData>>
- Streak provider uses per-day targets from target_history via asyncExpand
- Code-gen passes, dart analyze clean
</success_criteria>

<output>
Create `.planning/phases/10-target-history-integration/10-02-SUMMARY.md` when done
</output>
